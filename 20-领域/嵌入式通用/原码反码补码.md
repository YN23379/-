---
type: 知识库
scope: 嵌入式通用
doc_type: 未分类
status: 已整理
evidence: 教材课程
tags: [嵌入式, 体系结构]
updated: 2026-09-17
---

# 原码、反码与补码

计算机里由于运算器、存储器的位数限制，二进制数的表示必须统一对应的位数
对于有符号数，符号也要占其中一位
**原码、反码、补码都是专门为解决有符号整数的表示和运算而设计的。**
原码表示的缺陷：
1.负数原码递增方向和真值递增方向相反，不利于加减法运算
2.数0的表示不唯一，破坏了编码的唯一性原则


### 三码对照（先看例子）

以 8 位为例，求 -5 的三种编码：

| 十进制 | 原码 | 反码 | 补码 |
|---|---|---|---|
| +5 | 0000 0101 | 0000 0101 | 0000 0101 |
| -5 | 1000 0101 | 1111 1010 | **1111 1011** |
| -1 | 1000 0001 | 1111 1110 | **1111 1111** |

规则：正数三码相同；负数反码 = 原码除符号位按位取反；**补码 = 反码 + 1**。

## 为什么用补码（三行看懂）

为解决这个问题提出了补码
1.负数和非负数在各自范围内补码和真值一样都是向右递增的，利于加减法运算
2.数0的补码表示只有一种方式，满足编码唯一性原则
3.补码能多表示一个负数，即n+1位补码所表示的最小值是$-2^

### 补码的数学本质：模运算

补码不是"发明出来的规则"，而是**模运算的自然结果**：

```text
n 位系统的模 = 2^n。减一个数 ≡ 加上它的补数（模 2^n）。
-x ≡ 2^n - x   (mod 2^n)

例：8 位系统算 5 - 3：
  5 + (2^8 - 3) = 5 + 253 = 258
  258 mod 256 = 2   ✓ 正确
```

所以**硬件上只需要一个加法器**：`a - b` 就是 `a + 补码(b)`，减法/带符号运算全部统一——
这就是补码统治所有现代 CPU 的根本原因（电路简单 = 便宜 + 快）。

### n 位补码的表示范围推导

```text
最大值：0111...1 = +2^(n-1) - 1
最小值：1000...0 = -2^(n-1)      ← 没有对应的 +2^(n-1)，所以负数多一个
0 只有一种表示：0000...0
```

**常见坑**：`-128` 的补码还是 `1000 0000`，对它取负（+128）**溢出**，结果还是 -128。

### 移位与溢出规则（面试常问）

| 操作 | 规则 | 例（8 位） |
|---|---|---|
| 左移 `<<` | 低位补 0，**相当于 ×2**（未溢出时） | 0001 0100 << 1 = 0010 1000 |
| 算术右移 `>>`（有符号数） | **高位补符号位** | 1111 0000 >> 1 = 1111 1000（-16→-8） |
| 逻辑右移 `>>`（无符号数） | 高位补 0 | 1111 0000 >> 1 = 0111 1000 |
| unsigned 溢出 | **回绕**（mod 2^n） | 255+1 = 0 |
| signed 溢出 | **未定义行为 UB** | 不要依赖它 |

> **有符号数的右移是算术右移还是逻辑右移？** C 标准说这是**实现定义**——
> 但所有主流编译器对有符号负数都做**算术右移**（保留符号）。
> 写跨平台代码时不要依赖这一点，用显式掩码处理。

### 与嵌入式强相关的一个细节

把 `int` 截断成 `char`：**保留低 8 位，重新按补码解释**：

```c
int a = 300;          /* 0x12C */
char c = a;           /* 低 8 位 0x2C = 44 */
char d = 513;         /* 0x201 → 低 8 位 0x01 = 1 */
```

这也是为什么跨函数传参、协议打包时推荐用 `int8_t/uint8_t`（stdint.h）显式声明宽度——
见 [[20-领域/C与C++/C语言/数据类型与存储类型.md|数据类型与存储类型]]。
n$,
补码加减法法则和实数相同，但最高位的进位需要被丢弃
补码表示范围有限，运算超过范围则会溢出（有专用检测手段）


正数的补码是它本身，对于负数补码：
求负数的补码时，原码符号位1不变，后面每一位都取反，最后再给末位加1即可，
负数补码的另一种算法：符号位1不变，数值位从右往左遇到的0以及第一个1不变，其余数值取反
这个方法避免了末尾加1可能带来的进位，可以快速通过原码得到补码，这种方法也是补码硬件逻辑电路实现的基础
