---
type: 索引
scope: 全库
doc_type: 未分类
status: 待验证
evidence: 不适用
tags: [启动, 编译构建, 调试]
updated: 2026-09-17
---

# 工作笔记记录规则

所有已经完成的操作、查到的资料、理解的知识点和实际遇到的问题都要及时记录到本目录。记录时按主题分类，不把不同层次的内容全部写在同一篇笔记中。

- 开发板接线、启动、串口、下载命令和现场日志记录到 `10-项目/<项目名>`。
- FreeRTOS、uC/OS-II、任务、调度器和实时性原理记录到 `20-领域/RTOS`。
- 处理器架构、多核启动、内存、System Manager和芯片资源记录到 `20-领域/芯片与平台-i.MX95`。
- 原理图、元器件、电源、接口电路和通用调试方法记录到 `20-领域/嵌入式通用`。
- 工具安装、编译器、SDK获取和命令使用方法记录到 `20-领域/工具与环境`。
- 启动镜像、Bin/ELF、Boot ROM、UUU、SD/eMMC和remoteproc记录到 `20-领域/芯片与平台-i.MX95/启动与烧录`。

本目录另外区分两类笔记：

- `项目档案`：只记录某个项目的实际过程，包括日期、板卡版本、接线、命令、日志、失败现象、排查和最终结果。项目结束后仍保留原貌，便于追溯，不要求写成通用教材。
- `知识库`：从项目中提炼出来、以后换板子或换芯片仍可能复用的知识。这里不按日期写流水账，而按概念组织内容，必须写清适用范围、原理、判断方法和来源。i.MX95专属知识可以保留，但要明确它是芯片/板卡特性，不能误当成所有嵌入式平台的通用规则。

从项目记录提炼知识时，不能直接整篇复制。先回答“这个知识离开当前项目后还能解决什么问题”，再保留原理、方法和证据；当前项目的路径、COM号、SW4状态和临时命令放在项目档案中，知识库只保留必要示例。

知识库建议按以下层次组织：

1. 嵌入式通用：启动、镜像、Bin/ELF、内存、调试、串口和下载的通用概念。
2. 芯片与平台：i.MX95的多核、System Manager、LM/TRDC、时钟、IOMUX和资源模型。
3. 启动与烧录：Boot ROM、SDP/SDPS、UUU、启动容器、SD/eMMC和remoteproc的工作机制。
4. RTOS：FreeRTOS任务、调度、通信、内存、移植和实时性测试。

项目档案中的结论只有在完成资料核对并标注适用范围后，才提炼到知识库；未经验证的猜测、特定板卡临时修改和失败命令不直接进入知识库。

每次记录区分已经完成、资料中看到但尚未验证、后续待处理三种情况。涉及实际硬件时，记录接线方式、开关状态、工具参数、现象和错误信息。不要把计划写成已经完成的结果。

进行新开发或使用新的芯片、开发板、SDK和工具前，先查找并阅读厂家官方资料。资料优先级为开发板用户手册和快速入门、芯片参考手册、SDK示例自带说明、官方应用笔记、BSP文档、工具官方文档，最后再参考社区经验。笔记中注明结论依据和适用板型，区分官方明确说明、根据源码得到的结论和仍需实机验证的判断。

## 深度要求：不停留在"是什么"，要讲清"为什么"

只记录"某配置是多少、某命令怎么用"是不够的。重要的知识点必须往深挖一层，回答四个问题：

1. **数值/行为由什么决定？** 找到决定它的变量、配置项或硬件资源。
2. **为什么这样规定？** 设计者的取舍是什么——是性能、内存、实时性、还是兼容性？对比另一个系统（如 uC/OS、STM32、Linux）在同一问题上的不同选择，说明为什么不同。
3. **原理是什么？** 往下追到数据结构、源码或硬件机制。例如"最大任务数"不能只答"看堆大小"，要推导 `堆大小 / (栈 + TCB)`，并说明就绪表按优先级分桶所以优先级数与任务数无关。
4. **代价是什么？** 任何设计都是取舍。灵活性换了什么（确定性/实时性/内存）？什么时候该选另一种方案（如静态创建 `xTaskCreateStatic`）？

写法上的要求：数值要有推导过程，结论要有对比对象，取舍要写清适用场景。引用源码时给出文件与字段名；引用官方文档时给出文档名与章节。

## 问题记录规范：七个要素不可少

遇到任何技术问题（报错、现象异常、行为与预期不符），**当天**记录到项目档案，格式包含以下七项。缺项的记录不算完成。

1. **是什么问题**：现象的准确描述，包括完整报错信息、异常输出、或"预期 X 实际 Y"。截图或原始日志贴进附录，不转述。
2. **能不能复现**：复现步骤（精确到命令与操作顺序）、复现概率（必现/偶发/仅一次）。不能复现的也要记录当时的操作序列——它本身是重要线索。
3. **初步判断原因，以及为什么这么判断**：这一项最重要。写下你怀疑的原因，并**写明判断依据**（哪条日志、哪个文档、哪次实验支持这个怀疑）。同时列出当时考虑过但排除的其他可能，和排除理由。这能防止事后"想当然"，也让别人能检验你的推理链。
4. **解决方案**：具体做了什么，精确到命令/代码/配置。
5. **过程记录**：按时间顺序记录尝试过什么、每步观察到的结果（包括失败的尝试——失败路径同样是有效信息，能帮后人排除歧路）。
6. **结果与调整**：第一次解决是否生效；不生效的话分析为什么、怎么调整的。
7. **最终结果与方案**：验证通过的方案，以及**验证依据**（什么现象证明它真的解决了，而不是碰巧好了）。

最后加一段**整个过程的分析**：根因是什么、暴露了认知上的哪个盲区、以后怎么提前避免、这个经验能否提炼成通用规则进知识库。

> 示例范式见 `10-项目/FRDM-IMX95-PRO/开发日志.md` 与
> `10-项目/FRDM-IMX95-PRO/Harpoon验证与复现.md` 第九节「踩过的坑与规避」——
> 每条都按"现象/根因/规避"三列组织，可直接对照模仿。

## 周报/汇报写作规范（2026-09-19 依用户范文定稿）

写周报、周汇报时照以下习惯，写完拿范文对照一遍再交：

1. **格式**：三大栏——本周工作 / 下周计划 / 其他事项。不加小标题、不加粗、不列 1.2.3. 编号。本周工作写 1~2 段连贯的话（可用分号串），下周计划一句一件事平铺。
2. **逻辑是时间线**：做了什么 → 之后做了什么 → 中途发现什么、为什么转向（转向要有原因，例如"汇报同步后发现不是在 M7 核上跑，而是要在 A55 的两个核上跑 FreeRTOS，之后查阅资料发现有官方方案"）→ 现在做到哪、卡在哪（"正在进一步解决问题"）。让读的人跟着事情发展走，而不是看分类清单。
3. **用词平实**：跑通、跑起来、踩坑记录、梳理、跟踪回复、告一段落，这类日常词。禁用：闭环、沉淀、体系化、赋能、基线、全链路、纸面验证、落地、范式、量化。技术名词只在必要时出现（如 Jailhouse、TRDC），出现后半句话带出它是干嘛的。
4. **状态诚实**：跑到哪说到哪。"跑是跑起来了，但还看不到打印，换个串口重编"就照这么写，不包装成"阶段性成果"。
5. **每句话讲清"做了什么+到了哪一步"**，带必要数字（几个核、几条问题），不堆术语，不熟这块的人也能顺着读下来。
6. 细节、命令、日志不进周报——在项目档案里，周报只留时间线和结论。

**范文**：`D:\工作汇报\FRDM-IMX95-PRO\周报\周报-2026-09-19.md`（本周定稿，以后照此风格）。

## 演讲稿写作规范

以后写汇报稿、讲解稿、演讲稿时，遵守以下要求。完整流程见技能 `my_speech`（`C:\Users\chen\Desktop\资料\My_skill\my_speech`）。

1. 一次汇报产出两份文档。一份是稿子，用于口述；一份是知识梳理，逐条解释稿中出现的名词、因果和可能被追问的问题，用于应对提问。两份分开保存，不混在一篇里。
2. 先列主链再写稿。稿子只讲主链、关键动作和前因后果，细节放到知识梳理。
3. 稿子只用逗号和句号，不使用破折号、双引号、单引号、分号、冒号、括号等书面符号，方便口述。
4. 语言精简，删掉只为衔接和转折的冗余句。
5. 第一次出现的专有名词必须当场用一句话解释，并交代它的来源和前因后果。不能让听众靠猜。例如提到 Quick Boot 数据，要说明它来自原厂启动镜像、缺了会怎样。
6. 保留讲述者本人的语气、叙述顺序和用词习惯，不改成书面报告腔。
7. 事实以项目档案、源码和实机结果为准，区分已验证、资料确认和推测。

<!-- related-generated -->
## 相关

- [[00-入口/维护说明.md|维护说明]]
- [[00-入口/README.md|库总入口]]
