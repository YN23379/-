---
type: 索引
scope: 全库
doc_type: 怎么做
status: 待验证
evidence: 不适用
tags: [协议, 体系结构, 启动, 进程与线程]
updated: 2026-09-17
---

# 笔记库重构记录（2026-09-17）

## 一、要解决的问题

原来笔记散在三处，互有重复和分歧，结构不统一，找不到东西：

| 位置 | 规模 | 问题 |
|---|---|---|
| `D:\笔记` | 68 篇 md / 24 MB | 有独立 `.git`、`.obsidian`；`技术栈/` 与 `知识库/` 两套并行目录，同一概念两个地方都有 |
| `F:\Backup\一些资料\笔记` | 135 篇 md | 与 `D:\笔记` **同一个 git 仓库**（同 remote、同 HEAD `f34df7d`），是另一份检出；多出 `Work/`（FRDM-IMX95-PRO 项目档案）、`ARM/` |
| `F:\aa\Typora` | 16 篇 md + 18 PDF + 191 图 / 99 MB | Typora 培训与 stage 笔记，与上面两处完全无关 |

另有 11 篇两边都改过、内容有分歧；若干 0 字节空笔记；`1.md`、`未命名.md` 之类无意义文件名。

## 二、采用的结构与依据

选了三套业界框架，**各管一层**，避免混用导致目录爆炸：

| 框架 | 管什么 | 出处 |
|---|---|---|
| **PARA** | 顶层分区（Projects / Areas / Resources / Archives） | Tiago Forte《Building a Second Brain》 |
| **Diátaxis** | 笔记**类型属性** `doc_type`（教程/怎么做/参考/原理），**不建目录** | [diataxis.fr](https://diataxis.fr/start-here/) |
| **Zettelkasten** | 知识层的写法：一篇一个概念、互相链接 | Luhmann 卡片盒方法 |

数字前缀（`00/10/20/...`）借鉴 **Johnny.Decimal**：排序稳定，每层留插入空间。

最终结构：

```
D:\笔记\
├─ 00-入口\     索引、看板、规则、维护说明、总入口 README
├─ 10-项目\     PARA Projects：FRDM-IMX95-PRO、PLC-微波炉、Stage-Modbus、Stage-PLSR、面试项目
├─ 20-领域\     PARA Areas：C与C++、Linux、计算机系统、数据结构与算法、RTOS、嵌入式通用、芯片与平台-i.MX95、通信协议、工具与环境
├─ 30-资源\     PARA Resources：面试题库、培训-尹家昌、参考资料
├─ 40-归档\     PARA Archives：合并前原件、旧索引、空笔记
├─ 90-附件\     散落图片
└─ 99-模板\     知识库笔记 / 项目档案笔记 / 开发日报 / 面试题
```

## 三、怎么做的（可复现）

**核心原则：不原地搬。** 先复制出暂存树 `D:\笔记-新`，验证通过后再换名。

| 步骤 | 脚本 | 产物 |
|---|---|---|
| 1 清点 + 逐文件比对 + 三处 zip 备份 | — | `build\notes-backup\20260917-*\` |
| 2 两个仓库各自 git 存档 | — | `D:\笔记` → `e8c79af`；F → `a31e74f` |
| 3 生成逐文件映射清单 | `build\tools\plan_restructure.py` | `映射清单.csv`（499 条）、`映射冲突候选.csv`、`映射冲突-需人工合并.txt` |
| 4 建暂存树（复制+去重+归档冲突） | `build\tools\apply_restructure.py --stage build` | `D:\笔记-新`（401 目标 + 12 归档） |
| 5 校验 | `apply_restructure.py --stage verify` | 401/401 存在、0 缺失 |
| 6 加 frontmatter | `build\tools\add_meta_and_index.py` | 163 篇加 YAML |
| 7 建索引/看板/模板 | `add_meta_and_index.py --index` | README、3 个 MOC、2 个 Bases、维护说明、5 模板 |
| 8 链接校验与修复 | `check_links.py` / `fix_links.py` | 264 条有效引用，自动修 16 处 |
| 9 换名 | `apply_restructure.py --stage swap` | `D:\笔记` = 新库；`D:\笔记-旧-20260917` = 旧库 |

**去重与合并规则**：按内容 md5 判定。86 组内容完全相同 → 只留 D 那份；12 组有分歧 → 取 mtime 较新的一份当工作副本，旧的存到 `40-归档/合并前原件/`，并在 `build\notes-inventory\合并任务.md` 列出差异行数。

## 四、遇到的问题与根因（按发生顺序）

| # | 现象 | 排查过程 | 根因 | 解决 |
|---|---|---|---|---|
| 1 | 映射报告里 `面试题与面经/` 下的散落文件全被兜底塞进 `40-归档` | 检查规则顺序，发现 `.*\.md$` catch-all 在具体规则之后无条件命中 | 用了**兜底规则**，把"没规则命中"和"确实是垃圾"混为一谈 | 删掉 catch-all，未命中一律进 `映射未覆盖.txt` 报错，逼自己补规则 |
| 2 | `ARM嵌入式系统开发教程.PDF` 没被 PDF 规则命中 | 对比大小写 | 正则没加 `re.IGNORECASE`，`.PDF` ≠ `.pdf` | 全部匹配加 `IGNORECASE` |
| 3 | **图片被抽平到 `90-附件\`**，附件从 57 涨到 249、项目文件从 111 掉到 45 | 看二级目录分布发现 `10-项目/PLC-微波炉` 文件数骤降 | 类型规则放在了目录规则**前面**，把 stage/项目目录里的图抽走了 | 改成**目录规则优先、类型规则只兜底**——图片必须跟着笔记走，否则相对路径全断 |
| 4 | 86 组"内容完全相同"的也各复制了一份进归档 | 归档目录里出现大量 `__from-F` 文件、总数对不上 | 归档逻辑没有按 verdict 分流 | 只在 `有分歧` 时才归档；相同内容靠 md5 判定直接去重 |
| 5 | `shutil.rmtree` 删暂存树报 `WinError 5 拒绝访问` | 定位到 `.git\objects\pack\*.idx` | git pack 文件带**只读属性**，Windows 下 rmtree 不自动改权限 | 加 `onerror` 回调先 `chmod S_IWRITE` 再删 |
| 6 | 链接校验报 29 处断链，其中 `Markdown.md` 一堆 `URL`、`./IAR/IAR.md` | 打开该文件发现它本身是 **Markdown 教程**，这些是代码块里的语法示例 | 校验脚本没有跳过围栏代码块，把示例当成了真链接 | 校验前用 `FENCE` 正则把代码块挖掉；行内代码里的 `[[` 也跳过 |
| 7 | **把 `模版/1.md` 当垃圾模板删了** | 换名后做"映射文件是否都在"的复核，发现 3 条缺失，逐条查内容 | 文件名 `1.md` 让我**只凭文件名判断**，实际内容是《培训营薪资与制度》（5470 字节：8200→9200-10700、试用期考评、福利、发展路径） | 从旧库恢复到 `00-入口\培训营薪资与制度.md` 并补 frontmatter，提交 `e345b3f` |

> 第 7 条是最该记住的教训：**删除前必须看内容，不能只看文件名。**

## 五、验证结果

- 新库映射目标 **401/401 全部存在，0 缺失**；499 条映射逐条核对通过。
- 相对链接：**264 条有效引用**，自动重定位 16 处；剩余 5 处是**重构前就存在**的历史断链（`10-项目\FRDM-IMX95-PRO\日报\2026-09-1x.md` 引用的 `资料\日期\资料路径.md` 在两个旧库里都没有）。
- git 历史连续：`e345b3f` ← `1a38e33` ← `e8c79af` ← `f34df7d`。
- 旧库 `D:\笔记-旧-20260917` 保留未动，可整体回退。
- `F:\Backup\一些资料\笔记` 已按确认删除；删除前另存 `F-笔记-删除前最后留档.zip`（9.4 MB）与 `F-obsidian配置留档\`。

## 五之二、第二轮：内容级整理（同日晚）

第一轮只做到"搬到位 + 加属性"，第二轮处理**内容层面**的重复、合并与命名。

### 1. 12 组"内容有分歧"的冲突 —— 逐条量化后只有 2 组要真合并

第一轮的对比脚本**按文件名匹配对应文件**，把 `数据类型` 对到了顶层的 `数据类型.md`，
而它真正对应的是 `C知识体系/数据类型.md`，结论是错的。改成用 `执行日志.csv` 里的
精确目标路径、并剔除 frontmatter 噪声后重算：

| 结论 | 数量 | 处理 |
|---|---|---|
| 保留版是超集（旧版没有独有内容） | 5 | 不动 |
| 仅措辞/格式差异（1–3 行） | 5 | 不动 |
| **真需要合并** | **2** | 见下 |

- **`20-领域/嵌入式通用/STM32基础.md`**：保留版讲内部域/备份域/PWR/临界区保护，
  归档版讲工程建立/GPIO 八种模式/RCC 时钟，**两边完全不重叠** → 真合并，
  并把 STM32 的 GPIO 模式表与 i.MX95 的 IOMUXC+RGPIO 分层做了对比说明。
- **`20-领域/C与C++/C知识体系/C基础.md`**：归档版独有的 15 行是"基本数据类型
  32/64 位对照表"，该表在 `C知识体系/数据类型.md` 里已有完整版本 → 按**去冗余**
  处理：**不复制回来**，只留一条链接。

也顺手修正了一个自己造出来的不一致：第一轮给 `C基础.md` 追加的说明写着"已删掉重复表"，
但脚本实际只追加了文字、**表格并没有删**。这一轮真删了。

### 2. `20-领域/C与C++` 内部三套并行目录 → 按语言分二级

原来是 `C与C++/` 根 + `C知识体系/` + `注意类知识点/` 三套并存，且有**文件名与内容不符**：

| 原文件名 | 实际内容 | 处理 |
|---|---|---|
| `基础.md` | C++（封装/继承/多态/虚函数） | → `C++/基础.md` |
| `词法结构.md`（根） | C++ `override` 关键字 | → `C++/关键字-override.md` |
| `内存管理（栈、堆、全局、代码区）.md` | open/close/文件描述符 | → 并入 `C语言/文件与IO.md` |
| `数据类型.md`（根） | 结构体对齐 + 数组输入 | → 拆开：对齐并入 `内存与对齐.md`，数组输入并入 `C语言/文件与IO.md` |
| `文件.md`（根） | 二进制模式一句 | → 并入 `C语言/文件与IO.md` |
| `注意.md` | malloc 强转/循环/短路/宏 | → 并入 `C语言/易错点与陷阱.md` |
| `C知识体系/C基础.md` | 字长 + 内存对齐 | → `C语言/字长与类型大小.md`（删重复表） |

合并前先确认**不是重复**：`c基础与进阶.md` 与 `C知识体系/C基础.md`+`c进阶.md`
的行重叠只有 2% / 6%，是不同主题（总览+排序 / 字长与类型 / 函数与宏），三篇都保留。

重构后 `20-领域/C与C++` 为：`C语言/`（11 篇）、`C++/`（2 篇）、
`内存与对齐.md`、`学习方法与解题技巧.md`、`README.md`（导航）。
原目录完整备份在 `40-归档/C与C++重构前/`。

### 3. 命名与空文件

- `日报/1.md` 与 `2026-09-15.md` 是**同一天的两个版本**（前者有具体文件名
  `flash-m7-gpio.bin`、`PDOR` 读数、J15 引脚、AN14120 版本号；后者是叙述版带链接），
  各有独有信息 → 合并为 `2026-09-15.md` 的"补充记录"节，不是简单删一个。
- `20-领域/工具与环境/暂时.md` 内容是线程池面试准备 → `30-资源/面试题库/线程池面试准备.md`。
- 三个只剩 frontmatter 的空存根（原 0 字节文件）已删除：
  `30-资源/参考资料/资料.md`、`40-归档/空笔记/IDE.md`、`40-归档/空笔记/未命名-Linux.md`。

### 4. 历史断链清零

日报里 5 处指向 `资料/日期/资料路径.md` 的链接，这些文件在两个旧库里都不存在。
改成纯文本 + "文件缺失，待补建"标注，避免留下永远修不好的链接。

**结果：283 条有效引用，断链 0。**

### 第二轮又踩到的三个脚本坑

| 现象 | 根因 | 解决 |
|---|---|---|
| 对比脚本得出"要合并 4 组"的错误结论 | 按**文件名**去全库找对应文件，重名时匹配到了错误对象 | 改用 `执行日志.csv` 里的精确目标路径 |
| 链接校验报 15 处断链，全在 C/C++ 的 README | 表格里的 wikilink 必须写成 `\|` 才不破坏表格，校验脚本没还原转义 | 解析前先把 `\|` 还原成 `|` |
| **"重新生成索引"两次都白做，真库 MOC 仍是旧的** | 生成脚本里 `STAGE = r"D:\笔记-新"` 是**写死的暂存路径**，换名上线后没有更新。脚本的 `write()` 里有 `os.makedirs(..., exist_ok=True)`，于是它**静默重建了 `D:\笔记-新`** 并把索引写进去；而 `collect()` 走的是那个空目录，MOC 内容也是空的 | 把 `STAGE` 改成 `os.environ.get("NOTE_VAULT", r"D:\笔记")`，删掉误建目录，重新生成到真库 |

> 第三个坑最关键：**脚本里写死的临时路径，在流程推进后必须一起改**，
> 而且"写文件前自动建目录"这种便利设计会把"路径早已失效"这种错误**掩盖成成功**。
> 判断是否成功要看**产物是否在预期位置**，不能只看脚本有没有报错。

## 五之三、第三轮：按"是不是知识库"分流 + 去重合

### 1. 工作汇报类内容移出知识库

判定标准：**写给领导按天/按阶段交差的，不是知识库；写给以后自己查的，才是。**
以下 10 个文件已移到库外 `D:\工作汇报\`（不再污染知识库，但内容不丢）：

| 移出内容 | 去处 |
|---|---|
| `10-项目/FRDM-IMX95-PRO/日报/`（5 篇每日总结） | `工作汇报\FRDM-IMX95-PRO\日报\` |
| `汇报稿-FreeRTOS上板历程.md` / `-主链.md` / `-知识梳理.md` | `工作汇报\FRDM-IMX95-PRO\汇报稿\` |
| `进度汇报-2026-09-17.md` | `工作汇报\FRDM-IMX95-PRO\进度汇报\` |
| `00-入口/培训营薪资与制度.md` | `工作汇报\个人\` |
| `40-归档/合并前原件/日报-1.__原09-15另一版本.md` | `工作汇报\FRDM-IMX95-PRO\日报\2026-09-15-另一版本.md` |

汇报稿里可复用的技术结论**并没有因此丢失**——GPIO 三层初始化、TRDC 所有权、
BL31 改写 PCNS/PCNP 这些内容在 `2026-09-15-SD启动GPIO权限问题最终结论.md` 和
`2026-09-17-理解-*` 系列里都有正式笔记。

反过来，两个**放错位置的真知识**归位了：

- `00-入口/STUDY_NOTE.md`（μC/OS-II + STM32 学习笔记）→ `20-领域/RTOS/μC-OS-II学习笔记-STM32F103C8T6.md`
- `00-入口/TRAINING_CAMP.md`（训练营学习笔记）→ `30-资源/培训-尹家昌/训练营学习笔记.md`

### 2. 去重合（先量化再合并）

写脚本按"行集合互相包含率"扫 `20-领域` 全部 70 篇，避免凭感觉合并：

| 重合对 | 处理 |
|---|---|
| `Linux基础与开发环境.md` 的内核段 ↔ `Linux内核.md`（**80%**） | 删掉前者里重复的 73 行内核说明，改为链接 |
| `C语言/基础与进阶总览.md` 的排序算法 ↔ 全局算法主题 | 抽成独立笔记 `20-领域/数据结构与算法/排序算法.md`，原处留链接 |
| `C语言/数据结构与算法.md` 的排序节 ↔ 新抽出的排序笔记（24%） | 该笔记是通用 CS 主题、本就不该在 C语言 下 → 移到 `20-领域/数据结构与算法/数据结构与算法总览.md`，并删掉重复的排序节 |
| `Linux驱动基础/驱动基础.md` 的设备分类 ↔ `Linux内核.md` | 重复的 3 行改为链接 |

顺带纠正两处**文件名与内容不符**：

- `20-领域/Linux/Linux系统编程/驱动设备.md` 实际是**看课清单 + 面试考点**
  → 改名 `20-领域/Linux/Linux学习路线与视频清单.md`
- 上面那篇 `C语言/数据结构与算法.md`（在 C语言 目录下讲通用数据结构）

**结果：`20-领域` 内容重合对 = 0。**

### 3. 看板文件说明

`00-入口/*.base` 是 Obsidian **Bases**（数据库视图），不是普通笔记。已把用法写进
`维护说明.md`；`看板-全部笔记.base` 扩成 4 个视图（全部/领域知识/项目档案/参考资料）。
不想要就删文件，没有依赖。

## 五之四、第四轮：精简文件数 + 标题一看就知道是哪部分知识

### 1. 一个文档被拆成多文件的，合并回一个

| 合并前 | 合并后 | 说明 |
|---|---|---|
| `RTOS/00-FreeRTOS基本概念.md` … `11-验证优先级抢占的方法.md`（12 个） | **`RTOS/FreeRTOS.md`**（111 KB） | 本来就是**一个文档**被拆成 12 个文件；同时把 `FreeRTOS移植学习.md`、`实时性.md` 也并进来 |
| `RTOS/RTOS.md`（实为 uC/OS-II 内核 API）+ `μC-OS-II学习笔记-STM32F103C8T6.md` | **`RTOS/uC-OS-II.md`**（47 KB） | 同一主题两个文件 |
| `面试题库/Linux面试题1.md` + `Linux面试题2.md` | **`Linux面试题.md`**（64 KB） | 一个文档被拆成 1/2 |
| `面试题库/面试题.md` + `leetcode.md` | **`算法题.md`** | 都是 LeetCode 题解 |
| `工具与环境/SSH、SCP与SFTP使用.md` + `开发环境.md` | **`SSH与文件传输.md`** | 后者内容是 scp 传输导致 CRLF 损坏 |
| `C++/基础.md` + `C++/关键字-override.md` | **`C++/基础.md`** | 只有一个关键字，不值得单独一篇 |

合并时把每篇的标题**自动降一级**（并跳过代码块，避免 `#include` 被改成 `##include`），
顶部生成目录，所以层次不乱。

**`20-领域/RTOS` 从 17 篇降到 3 篇**（FreeRTOS / uC-OS-II / README）。

### 2. 名字看不出内容的，改成"简短但一看就知道是哪部分知识"

| 原名 | 新名 | 实际内容 |
|---|---|---|
| `RTOS/RTOS.md` | `RTOS/uC-OS-II.md` | 内容是 uC/OS-II 内核 API，不叫 RTOS |
| `工具与环境/API.md` | `工具与环境/API概念.md` | API 是什么、三要素 |
| `工具与环境/Obsidian.md` | `工具与环境/Obsidian数学公式.md` | Obsidian 的 LaTeX 数学块 |
| `工具与环境/MobaXterm.md` | `工具与环境/MobaXterm使用.md` | 串口工具用法 |
| `嵌入式通用/原码.md` | `嵌入式通用/原码反码补码.md` | 有符号数三种编码 |
| `嵌入式通用/ARM/ARM.md` | `嵌入式通用/ARM架构.md` | ARM 架构（并去掉多余一层目录） |
| `计算机系统/CPU.md` | `计算机系统/CPU架构与指令集.md` | CISC/RISC、冯诺依曼/哈佛 |
| `Linux/Android.md` | `Linux/Android系统架构.md` | Android 分层与启动流程 |
| `00-入口/个人要求与约定.md` | `00-入口/能力要求清单.md` | 技能要求清单 |
| `面试题库/计算机网络原理面试题.md` | `面试题库/计算机网络面试题.md` | 标题精简 |
| `面试题库/笔试.md` | `面试题库/笔试基础题.md` | c/c++/Linux/OS/ARM/MCU 笔试 |
| `面试题库/实际经历.md` | `面试题库/笔面试实际经历.md` | 各公司实际笔试题 |
| `面试题库/面经.md` | `面试题库/面试八股.md` | 内存/Linux/OS/C 语言八股 |
| `面试题库/简答.md` | `面试题库/自我介绍与非技术问答.md` | 自我介绍与 HR 问题 |
| `面试题库/流程（待完善）.md` | `面试题库/面试自述流程.md` | 自述脚本与面试点 |
| `面试题库/线程池面试准备.md` | `面试题库/面试专题深化计划.md` | 线程池/MQTT/状态机/Flash 四个专题 |
| `面试题库/README.md` | `面试题库/电子书网盘链接.md` | 内容其实是网盘分享，不是 README |

**归位**：`开发思路与技巧.md`（模块间通信设计）→ `20-领域/嵌入式通用/模块间通信设计.md`；
`MCUXpresso SDK获取.md` → `10-项目/FRDM-IMX95-PRO/`（项目专属步骤）。

### 3. 清理归档冗余

改名/合并时"先备份再改"会在 `40-归档/合并前原件/` 留下大量**与库内内容完全相同**的副本。
已按内容哈希比对，删掉 21 个纯副本，保留 36 个内容确有分歧的旧版本；
`40-归档/C与C++重构前/`（22 个文件）打包成 `C与C++重构前.zip`。

**库内 Markdown：183 → 162 篇；真正在用的（去掉 40-归档）124 篇。**

### 4. 又踩到的坑

| 现象 | 根因 | 解决 |
|---|---|---|
| `RTOS/README.md` 里的"项目档案"链接被改成了 `面试题库/电子书网盘链接.md` | 链接修复脚本**按文件名匹配**，`README` 这个名字全库到处都是，`README→电子书网盘链接` 的改名把不相干的 README 链接一起改了 | 修回；并给脚本加"通用名（README/基础/面试题…）不参与自动改写"的黑名单 |
| 漏扫出 2 处旧路径链接 | PowerShell `-Path 'D:\笔记\**\*.md'` 的 `**` **不递归**，只扫到一层目录 | 改用 `Get-ChildItem -Recurse -Filter *.md \| Select-String` |

## 五之五、第五轮：项目档案同样精简

`10-项目/FRDM-IMX95-PRO` 里也有"一个主题拆成多篇"的情况：

| 合并前 | 合并后 |
|---|---|
| `理解-01 启动逻辑与资源隔离` + `理解-02 上电启动全流程超详细` + `理解-03 外设归属与权限矩阵`（**原本就编号 01/02/03，互相交叉引用**） | **`理解-i.MX95启动与资源隔离.md`**（71 KB） |
| `Harpoon可用性验证` + `Harpoon全过程与复现步骤` | **`Harpoon验证与复现.md`**（43 KB） |
| `2026-09-14/15/16/17 开发日志`（4 个按日拆分的文件） | **`开发日志.md`**（按日期分节的连续日志，43 KB） |

话题性笔记去掉冗余日期前缀，标题缩短：

| 原名 | 新名 |
|---|---|
| `2026-09-15-SD启动GPIO权限问题最终结论.md` | `SD启动GPIO权限问题结论.md` |
| `2026-09-17-JTAG-SWD接口调研.md` | `JTAG与SWD接口调研.md` |
| `2026-09-17-待向NXP确认的问题清单.md` | `待向NXP确认的问题清单.md` |
| `2026-09-17-资料清单表.md` | `资料清单表.md` |
| `2026-09-17-A55-FreeRTOS-新要求与时间安排.md` | `A55-FreeRTOS任务与时间安排.md` |
| `方向调整-A55运行FreeRTOS.md` | `A55运行FreeRTOS的方向调整.md` |

**`10-项目` 从 32 篇降到 26 篇。**

### 又踩到的坑

脚本把"合并目标"写成了**文件名**而不是 **vault 相对路径**，导致对外链接被算成 `../../开发日志.md`
（少了一层 `10-项目/FRDM-IMX95-PRO/`）。已修脚本，并用"按文件名全库重定位"的方式修好 3 处链接。

### `evidence` 标注说明

在 `维护说明.md` 里补了一节「`evidence` 到底要标什么」，写清四档取值
（`官方资料` / `源码确认` / `实机验证` / `推测`）、判断口径、每篇 10 秒的做法和为什么值得做。

## 五之六、第六轮：让库真正好用（链接、索引、Obsidian 配置）

### 1. 问题：一半笔记是"死胡同"

体检发现 **105 篇里有 59 篇完全没有对外链接**——打开一篇就走进死胡同，
既不知道它属于哪块知识，也不知道还能看什么。索引虽有 MOC，但只是文件名清单。

### 2. 补链接与导航

| 做法 | 结果 |
|---|---|
| 每个 **≥3 篇**的目录生成导航页 `README.md`（列出本目录笔记 + 一句话说明 + 上下级链接） | 8 个；少于 3 篇的目录**不生成**，免得为 1 篇笔记加 1 个文件 |
| 给对外链接 < 2 条的笔记在末尾补「相关」节（同目录相似 3 条 + 跨目录相关 2 条 + 导航链接） | 74 篇 |
| 重写 MOC：加"一句话说明"和"依据"两列，按目录分组 | MOC-项目/领域/资源 |

**内部引用从 286 条涨到 606 条**，0 断链。

踩的两个坑：

- 一开始用正文里的 `# ` 当标题，结果出现「1. 反汇编代码段」「创建Swap分区步骤：」这种——
  因为很多笔记的 `# ` 是**章节标题**不是文件标题。改成**一律用文件名**（文件名已经是短而准的）。
- 跨目录"相关"用标题相似度硬配，配出了 `Linux内核 ↔ PLC_HMI|TouchWin` 这种无关联的。
  改成必须 **scope 相同或有共同 tag**，且相似度 ≥ 0.12。

### 3. 预填 `evidence`（A 方案）

按内容自动判定并写了 `evidence预填报告.csv`（逐条记录命中依据，可抽查）：

| 依据 | 篇数 | 判据 |
|---|---|---|
| `实机验证` | 28 | 正文有实测/串口日志/波形/寄存器读数 |
| `教材课程` | 46 | 学习类笔记、面试题库、培训笔记（非官方文档也非实测） |
| `不适用` | 31 | 清单/计划/索引/导航页这类不是技术结论的文件 |
| `源码确认` | 19 | 引用了 TF-A / U-Boot / imx-sm / 配置头文件等 |
| `官方资料` | 2 | 引用 AN/UM/RM/数据手册 |

> 说明：一篇笔记里依据常常不止一种，按约定**取最强的那档**，所以"官方资料"篇数偏少
> （很多同时有官方文档和实测的，归到 `实机验证`）。要改随时改，依据都在报告里。
>
> 预填过程中修正了三处启发式误判：`RM` 没加词边界会撞英文单词；`手册` 只要提到就命中；
> 计划类正则里的"目录/索引"误伤了技术正文（`dentry 目录项`、`线性索引查找`）。

### 4. Obsidian 配置（写进 `.obsidian\`，重启生效）

| 配置 | 值 | 为什么 |
|---|---|---|
| 自动更新内部链接 | 开 | 移动/改名笔记时链接自动跟着改，这是知识库最关键的一条 |
| 附件目录 | `90-附件` | 贴图不再散落 |
| 新笔记位置 | 当前目录 | |
| 链接格式 | wikilink | 全库统一 |
| 模板文件夹 | `99-模板` | 命令面板直接插模板 |
| 书签面板 | 5 组 | 每天要看／索引／规矩与记录／当前项目／常查知识 |
| 关系图配色 | 按分区上色 | 绿=项目、蓝=领域、橙=资源、紫=入口 |
| 库内日记插件 | 关闭 | 日报在库外 `D:\工作汇报\`，避免误建 |

插件建议单独写了一篇：`00-入口\Obsidian使用与插件建议.md`。
第一批推荐 **Obsidian Git**（自动提交备份，库本来就是 git 仓库）、**Homepage**（启动打开总入口）、
**Linter**（保证六个属性格式一致）、**Advanced Tables**（本库表格多）。
没替你装——第三方插件是在你库里跑的代码，按名字确认作者后再装更稳妥。

## 六之补、第七轮：图片路径 + 人工 MOC + 横切标签（2026-09-18）

用户反馈三点：知识库效果仍不够、图片路径是旧路径、链接不清晰、分类不完整。

### 1. 图片路径修复

库里有三种图片引用，其中两种 Obsidian 显示不出：

| 类型 | 数量 | 问题 | 修法 |
|---|---|---|---|
| 反斜杠路径 `pictures\xxx.png` | 77 | Typora 导出遗留，Obsidian 只认 `/` | 改正斜杠（改前逐处验证 100% 命中） |
| 短名内嵌 `![[xxx.png]]` | 28 | 图片移入 `90-附件/` 后，10 个重名图会歧义 | 改成带位置 `![[90-附件/xxx.png]]` |

修复后：**122 处图片/内嵌引用 0 失效**。

### 2. 链接不清晰 → 人工 MOC

之前"## 相关"是算法按标题相似度硬配，看不出"为什么看这篇"。改为**每个主题手写一张地图**
（范围 + 建议阅读顺序 + 笔记关系 + 相邻主题），共 12 个：RTOS、Linux、Linux系统编程、
C与C++、C语言、嵌入式通用、芯片与平台-i.MX95、启动与烧录、计算机系统、数据结构与算法、
通信协议、工具与环境。

### 3. 分类不完整 → 横切主题标签

原来 43 个碎标签（34 个只用 1 次，且多是目录名/文件名），收敛为 **18 个受控横切主题**
（内存管理、进程与线程、调度、同步与IPC、中断、文件系统、网络、驱动、编译构建、调试、
时钟、启动、电源、存储、算法与数据结构、安全与隔离、多核与异构、体系结构）。
每篇按关键词命中数取 top3 打标，域标签（C语言/Linux/RTOS…）保留。这样"内存管理"能跨
C/Linux/计算机系统检索到一起——这才是标签的"多维"价值。

> 纯关键词自动打标的教训：第一版关键词太宽（"内存""文件"这种词几乎每篇都有），
> "内存"标了 92 篇；改成"按命中次数排序取 top3"后才收敛。标签是辅助检索，
> scope 目录才是主分类，不必追求机器完美。

### 4. 归位

- `学习方法与解题技巧.md`（算法方法论）从 `C与C++/` 移到 `数据结构与算法/`
- `内存与对齐.md`（结构体对齐）从 `C与C++/` 移到 `C与C++/C语言/`
- 同步修 3 处旧路径引用、scope、噪声标签

## 七、遗留与后续

1. `evidence` 已预填完毕（无 `待标注`）。想核对就打开 `evidence预填报告.csv` 看每篇的判定依据，
   不同意的直接改笔记里的 `evidence:` 即可。
2. `40-归档\合并前原件\` 里是冲突旧版本与已合并的原件，可再清理。
3. `F:\aa\Typora` 源目录未动（其中 `Typora\` 是应用本体，不能删）；已导入新库。
4. `D:\工作汇报\` 是库外的工作汇报存放处；若确认不需要留存可直接删该目录。
5. `D:\笔记-旧-20260917` 是重构前的完整旧库，确认无误后可删。
6. 重启一次 Obsidian，让新配置（书签、模板目录、关系图配色、标签）生效。

<!-- related-generated -->
## 相关

- [[00-入口/维护说明.md|维护说明]]
- [[00-入口/Obsidian使用与插件建议.md|Obsidian 使用与插件建议]]
- [[00-入口/README.md|库总入口]]
