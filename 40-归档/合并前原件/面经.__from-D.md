---
type: 归档
scope: 全库
doc_type: 参考
status: 已归档
evidence: 待标注
tags: []
updated: 2026-09-17
---

## 八股
### 内存管理
### 堆和栈的区别
（1）管理方式不同：
栈直接由编译器管理（产生和消除），堆由程序员管理，程序员管理其的产生和消除
（2）空间大小不同：
栈占用的空间较小，而堆占用的空间较大
（3）能否产生碎片不同：
栈不会产生碎片，但是堆会产生，会有内存泄露的问题
（4）生长方向不同：
栈是向下压栈，堆是向上存放数据
（5）分配方式不同：
栈是在程序员申请之后，由系统分配的没有经过初始化的变量，只有动态分配方式。而堆是由程序员自己实例化，创建的已经过初始化的变量，分配方式类似于链表，动态分配和静态分配都可以
（6）分配效率不同：
栈是由内存分配的，系统专门为其准备寄存器，同时有专门的出栈和入栈指令，因而效率比较高。而堆空间则是C库分配的，可能会存在碎片的原因导致内存不连续，因而效率比较低
#### 内存泄漏的原因和怎么排查，如何避免？
#### 栈溢出会产生什么问题
#### 怎么检测到栈溢出，如何规避




### Linux基础
#### 进程和线程的区别





### 操作系统
#### 操作系统启动流程

#### 中断和异常的区别

### 软硬链接

### c语言

#### 全局变量和局部变量同名，编译器是怎么处理调用的

#### 怎么定义常量的
`#define或者const`

1、define是预编译指令，const是普通变量的定义，define定义的宏是在预处理阶段
展开的，而const定义的只读变量是在编译运行阶段使用的。

2、const定义的是变量，而define定义的是常量。define定义的宏在编译后就不存在了，它不占用内存，因为它不是变量，系统只会给变量分配内存。但const定义的常变量本质上仍然是一个变量，具有变量的基本属性，有类型、占用存储单元。所以编程时在使用const和define都可以的情况下尽量使用常变量来取代宏。

3、const定义的是变量，而宏定义的是常量，所以const定义的对象有数据类型，而宏定义的对象没有数据类型。所以编译器可以对前者进行类型安全检查，而对后者只是机械地进行字符替换，没有类型安全检查。这样就很容易出问题）

`应该还有程序运行时创建的字符串常量这些？`

#### 函数指针的用途，回调函数怎么实现


#### 悬空指针和野指针的区别

#### 数组
1. **两数之和**（哈希表/双指针）
2. **删除有序数组中的重复项**（快慢指针）
3. **寻找数组的中心下标**（前缀和）
找到数组的一个位置，其左侧所有元素和等于右侧所有元素和
// 关键：总和 = 左边和 + 当前值 + 右边和
4. **旋转数组**（三次反转）

// 问题：将数组向右旋转k个位置
// 最优解：先整体反转，再反转前k个，最后反转剩余部分
// 时间复杂度O(n)，空间复杂度O(1)
5. **合并两个有序数组**（从后向前）

// 问题：将nums2合并到nums1中，nums1有足够空间
// 技巧：从nums1和nums2的末尾开始比较，从nums1的最后开始填充
6. **最大子数组和**（动态规划/贪心）

// 问题：找出连续子数组的最大和
7. **移动零**（快慢指针变种）

// 问题：将数组中的所有0移动到末尾，保持非零元素相对顺序
8. **颜色分类（荷兰国旗问题）**（三指针）

// 问题：原地排序只包含0、1、2的数组
// 解法：三个指针left, curr, right
// left指向0的右边界，right指向2的左边界，curr遍历
9. **盛最多水的容器**（双指针）

// 问题：找出两条线，使其与x轴构成的容器能容纳最多的水
// 解法：左右指针向中间移动，每次移动较短的那一边
10. **下一个排列**（找规律）

// 问题：找到数组的下一个字典序更大的排列
// 三步：1.从右找第一个升序对 2.交换 3.反转后面部分
11. **数组作为函数参数**

// 面试常见问题：
// 1. `sizeof(arr)` 在函数内外的区别
// 2. 数组名作为指针传递
`void func(int arr[])` {
    // `sizeof(arr)` 是指针大小，不是数组大小！
}
12. **内存布局问题**
// 二维数组的内存布局
`int arr[3][4]`;
// 内存中是连续的12个int
// `arr[i][j] 的地址：&arr[0][0] + i*4 + j`
13. **环形缓冲区实现**（嵌入式高频）
```c
// 面试常考：用数组实现环形队列
struct CircularBuffer {
    int buffer[SIZE];
    int head; // 写指针
    int tail; // 读指针
    int count; // 当前元素数
};
// 关键：判断空/满的条件
```

### 网络
#### tcp/udp区别,头部分别是多少字节
头部分别是20字节和8字节

#### DNS服务是什么

#### MQTT相关

##### 怎么确保连接的稳定性，如果连接中断怎么恢复
为保证连接稳定性，设备端与云端建立 MQTT 连接时，会设置保活心跳包机制，设备按一定时间间隔（如 30 秒）向云端发送心跳包，云端收到后回复确认，若设备一段时间未收到确认，就判定连接可能中断，主动重连。

遇到网络中断，系统首先尝试重连网络，重连成功后，从上次断点处继续升级。这需要在升级前记录已接收数据包的序号、升级进度等信息，重连后向云端请求补发缺失数据包，利用 MQTT 的 QoS（服务质量）机制确保数据可靠传输，保障升级完整性。

##### MQTT协议的Qos等级介绍

QoS 0 只管发送，不保证接收，对于固件升级这种对数据完整性要求高的场景风险太大；

QoS 2 虽保证最强可靠性，但带来较高的开销与延迟，因需多次握手确认，在网络条件一般时会影响升级效率。

而 QoS 1 提供 “至少一次” 的送达保证，即消息若丢失会重发，既满足固件升级对数据不丢失的要求，又在一定程度上兼顾了实时性，

# 代码题
## 内存占用

### `int**a[3][4]`数组占多少字节

### `a[1][4]`和`a[4]`区别
`a[1][4]`是二维数组，`a[4]`是一维数组，两者的内存布局是一样的，但是类型不同
在指针运算，传递参数的时候也不同
```c
int a[1][4];
// 错误：
int* p = a;  // 编译警告：类型不兼容
// 正确：
int (*p)[4] = a;  // 指向"包含4个int的数组"的指针
```



### 结构体和结构体指针的区别，用什么好
内存占用的区别，结构体需要占用内部所有元素大小的字节，指针只需要占用指针大
小的字节；我选择指针，空间占用小，在知道指针大小的情况下一般不会发生调用问
题

## 函数实现

memmove函数实现
### 字符串函数实现







## 其他
知道一些设计模式吗

懂不懂Linux以及多线程和多进程的硬件支持。

