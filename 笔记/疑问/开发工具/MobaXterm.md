### **终端容易跳行以及 `stty` 的作用**
**根本原因：终端会话的**行列数**与实际终端窗口大小不匹配。**
使用stty设置窗口大小
或者stty sane将所有的终端设置恢复为**合理默认值**。
或者stty onlcr -ixon
- `onlcr`：将输出中的换行`\n`转换为回车换行`\r\n`
    
- `-ixon`：禁用软件流控制（避免 Ctrl+S 冻结终端）