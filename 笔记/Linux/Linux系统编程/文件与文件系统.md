#文件


### 系统调用

####  wait与waitpid
用于回收子进程资源
```c
// 方式1：wait() - 阻塞等待
pid_t wait(int *status);

// 方式2：waitpid() - 更灵活，可以非阻塞
pid_t waitpid(pid_t pid, int *status, int options);
// 使用 WNOHANG 选项可以非阻塞检查
```

### flock






