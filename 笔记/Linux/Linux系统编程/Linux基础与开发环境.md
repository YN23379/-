
# 开发环境
### Shell

Shell是一个**命令解释器**，它用于接收并执行用户输入的命令。
执行脚本（一系列命令的集合）。
提供编程功能，如变量、循环、条件判断。
常见：Bash, Zsh, Ksh, Csh等。
#### shell脚本

shell 是一种脚本语言  而脚本 本质是一个文件（.sh结尾，也可以不写），文件里面存放的是 特定格式的指令，系统可以使用脚本解析器 翻译或解析 指令 并执行（它不需要编译）  
**shell 既是应用程序 又是一种脚本语言**，查看自己linux系统的默认解析：
echo $SHELL $SHELL是环境变量
脚本的调用：打开终端时系统自动调用：/etc/profile 或 ~/.bashrc
**/etc/profile**
此文件为系统的每个用户设置环境信息,当用户第一次登录时,该文件被执行，系统的公共环境变量在这里设置
开机自启动的程序，一般也在这里设置
**~/.bashrc**
用户自己的家目录中的.bashrc
登录时会自动调用，打开任意终端时也会自动调用
这个文件一般设置与个人用户有关的环境变量，如交叉编译器的路径等等
用户手动调用：用户实现的脚本

##### shell语法
在脚本文件.sh中：
定义开头：#!/bin/bash。**#!用来声明脚本由什么类型的shell解释，否则使用默认shell**
加上可执行权限：在终端上，chmod +x xxxx.sh
运行方式：./xxx.sh bash  xxx.sh . xxx.sh
./xxx.sh 按脚本文件里#!指定类型的shell解析脚本
bash xxx.sh 优先用bash解析，没有就文件指定的
. xxx.sh 直接使用默认解析器解析（不会执行第一行的#！指定的解析器）但是第一行还是要写的

变量：
变量名=变量值  ，**等号两边不能有空格！**
$： 引用变量，
echo $变量名,等价于 echo ${变量名}  打印变量的值
unset  变量名  （用于清除变量值）
传递参数：脚本内通过 `$1`, `$2` ... 获取执行脚本时传入的参数。
`$0`是当前执行脚本的名称，$SHELL是当前系统默认使用的Shell路径
read 变量名  变量获取键盘输入的值，可多个读取，变量名直接空格隔开
readonly 变量名 声明只读变量

终端上输入env查看环境变量，
导出环境变量 作用：（让其他shell脚本识别该变量，设为全局变量）
source FileName 在当前bash环境下读取并执行FileName中的命令。
该命令通常用命令“.”来替代：. FileName

![[Shell变量生命周期.png]]
1. **`source`（或`.`）** 是在**当前Shell进程**内部执行脚本，所以脚本里定义的**所有变量（无论是否export）** 在当前终端都有效。
    
2. **`./script.sh`** 是启动一个**新的子Shell进程**来执行脚本。脚本结束时，这个子进程就被销毁了，它里面定义的**所有变量也随之销毁**，所以在父Shell（终端）里看不到。
    
3. **`export`** 的关键作用是：**将一个变量标记为“环境变量”，使其可以被子进程继承。** 如果不用`export`，子进程根本无法从父进程那里知道这个变量的存在。
想让脚本的变量**影响你的当前终端环境**，用 **`source`**。
想让脚本内部的变量**被它自己启动的程序（子进程）使用**，用 **`export`**。
若需要让变量永久有效则需要写在bashrc文件，或者是bash_profile中，shell会在启动时读取这两个配置文件，最好是在末尾添加，而根目录下的etc目录里的profile和bashrc文件则是对所有用户都有效
**注意事项**：
1、变量名只能包含英文字母下划线，不能以数字开头
1_num=10 错误
num_1=20 正确
2、等号两边不能直接接空格符，若变量中本身就包含了空格，则整个字符串都要用双引号、或单引号括起来
3、双引号 单引号的区别
双引号：可以解析变量的值
单引号：不能解析变量的值
4.**空格就是生命线**：

- `if` 和 `[[` 之间要有空格
    
- `[[` 和变量之间要有空格
    
- 变量和运算符（如 `-eq`）之间要有空格
    
- 运算符和变量之间要有空格
    
- `]]` 和前一个变量之间要有空格


![[预设变量.png]]

![[脚本变量特殊用法.png]]
![[shell比较.png]]
![[命令行控制.png]]

##### 控制语句
if
格式一：
if [条件1]; then
    执行第一段程序
else
    执行第二段程序
fi
格式二：
if [条件1]; then
    执行第一段程序
elif [条件2]；then
执行第二段程序
else
    执行第三段程序
fi

![[case.png]]
**for循环**
![[for循环.png]]


![[for循环1.png]]

**while循环**
![[while循环.png]]





#### bash
shell的一种（**Bourne-Again SHell**）
是**最流行、最常用**的一种，是大多数Linux发行版和macOS的默认Shell。

### Makefile
Makefile是一个**文本文件**，它包含了如何**编译和链接**一个程序的规则，用 `make` 工具来使用。
主要用于**智能增量编译**，自动化地完成编译过程


### GDB
#### **1. 启动与退出**

| 命令                               | 简写  | 说明            |
| -------------------------------- | --- | ------------- |
| `gdb <program>`                  | -   | 启动GDB并加载可执行文件 |
| `gdb --args <program> <args...>` | -   | 启动GDB并设置程序参数  |
| `gdb -p <pid>`                   | -   | 附加到正在运行的进程    |
| `quit`                           | `q` | 退出GDB         |
#### **2. 设置断点**

断点是调试的基石，让程序在指定位置暂停。

| 命令                         | 简写    | 说明            |
| -------------------------- | ----- | ------------- |
| `break <location>`         | `b`   | 在指定位置设置断点     |
| `break file.c:linenum`     | `b`   | 在指定文件的指定行设置断点 |
| `break function_name`      | `b`   | 在指定函数入口设置断点   |
| `info breakpoints`         | `i b` | 列出所有断点        |
| `delete <breakpoint-num>`  | `d`   | 删除指定编号的断点     |
| `delete`                   | `d`   | 删除所有断点        |
| `disable <breakpoint-num>` | `dis` | 禁用断点（不删除）     |
| `enable <breakpoint-num>`  | `en`  | 启用断点          |
#### **3. 运行程序**

|命令|简写|说明|
|---|---|---|
|`run`|`r`|开始执行程序|
|`run <arg1> <arg2> ...`|`r`|带参数运行程序|
|`start`|`-`|停在 `main` 函数的第一行（方便在程序开始时暂停）|
|`continue`|`c`|从当前断点处继续运行，直到下一个断点或程序结束|
|`step`|`s`|**单步步入**：执行下一行代码，如果遇到函数调用，会进入函数内部|
|`next`|`n`|**单步步过**：执行下一行代码，但将函数调用当作一步执行，不进入内部|
|`finish`|`fin`|**步出**：继续运行，直到当前函数执行完毕并返回|
#### **4. 查看与修改变量**

| 命令                           | 简写       | 说明                               |
| ---------------------------- | -------- | -------------------------------- |
| `print <expression>`         | `p`      | 打印表达式（变量、结构体等）的值                 |
| `print *ptr@len`             | `p`      | 打印指针指向的数组（`ptr` 是指针，`len` 是元素个数） |
| `display <expression>`       | `disp`   | 每次程序暂停时，自动打印指定表达式的值              |
| `info display`               | `i disp` | 查看所有自动显示表达式                      |
| `undisplay <num>`            | `undisp` | 取消自动显示                           |
| `set var <variable>=<value>` | `-`      | 修改变量的值（如 `set var i=0`）          |
| `whatis <variable>`          | `-`      | 查看变量的类型                          |
| `ptype <variable>`           | `-`      | 详细查看变量的类型定义（如结构体）                |
|                              |          |                                  |


#### **5. watch**

|命令|简写|说明|
|---|---|---|
|`watch <expression>`|`wa`|设置**写监视点**。当表达式的值**被写入（改变）**时暂停。|
|`rwatch <expression>`|`rwa`|设置**读监视点**。当表达式的值**被读取**时暂停。（较少使用）|
|`awatch <expression>`|`awa`|设置**访问监视点**。当表达式的值**被读取或写入**时暂停。（较少使用）|
|`info watchpoints`|`i wa`|列出所有当前设置的监视点。|
#### **6. 调用栈与上下文**

调用栈记录了函数调用的路径。

| 命令                  | 简写        | 说明                    |     |
| ------------------- | --------- | --------------------- | --- |
| `backtrace`         | `bt`      | 显示当前的函数调用栈（最重要！）      |     |
| `backtrace full`    | `bt full` | 显示调用栈，并打印每个栈帧的局部变量    |     |
| `frame <frame-num>` | `f`       | 切换到指定的栈帧（上下文）         |     |
| `info frame`        | `i f`     | 详细描述当前栈帧（局部变量、寄存器地址等） |     |
| `info locals`       | `i lo`    | 打印当前栈帧的所有局部变量         |     |
| `info args`         | `i ar`    | 打印当前函数的参数             |     |
#### **7. 内存与寄存器**

| 命令                            | 简写    | 说明                 |
| ----------------------------- | ----- | ------------------ |
| `info registers`              | `i r` | 查看所有寄存器的值          |
| `print $rax`                  | `p`   | 查看特定寄存器（如 `rax`）的值 |
| `x/<format> <address>`        | `x`   | 检查指定内存地址的内容        |
| `set {type}<address> = value` | `-`   | 向内存地址写入值           |

#### **8. 多线程调试**

|命令|简写|说明|
|---|---|---|
|`info threads`|`i th`|列出所有线程|
|`thread <thread-id>`|`th`|切换到指定线程|
|`break location thread thread-id`|`b`|在指定线程的指定位置设置断点|
|`set scheduler-locking on`|`-`|锁定调度器，使只有当前调试的线程会运行|
#### **9. 核心转储分析**

|命令|简写|说明|
|---|---|---|
|`gdb <program> <core-file>`|-|加载可执行文件和核心转储文件进行分析|
|`where` / `backtrace`|`bt`|**第一时间使用！** 查看程序崩溃时的调用栈|
|`print`|`p`|检查崩溃时变量的值|
|`info registers`|`i r`|查看崩溃时的寄存器状态|
解core，


# Linux基础


## Linux系统

### Linux系统组成
标准架构：Bootloader->Kernel->Rootfs->Application

#### **Bootloader**：
Bootloader 是一段在操作系统内核运行之前执行的小程序。它负责初始化硬件设备、建立内存空间映射，配置好正确的环境，从ROM中加载内核映像到内存，将控制权转交给内核。
Bootloader存储在ROM中。

常见类型：
- **U-Boot**：嵌入式 Linux 领域事实上的标准，功能强大，支持多种架构。
    
- **GRUB**：主要用于 x86 PC 的 Linux 系统。
    
- **RedBoot**：基于 eCos 的 Bootloader。

**核心工作流程：**

1. **硬件初始化**：初始化 CPU 时钟、SDRAM 控制器、串口等关键硬件。
    
2. **环境建立**：为操作系统内核设置好运行时环境，尤其是内存布局。
    
3. **内核加载**：从存储设备（如 eMMC、Flash）将内核映像和设备树文件加载到指定的内存地址。
    
4. **传递参数**：根据约定，向内核传递启动参数，如内存大小、控制台设备、设备树在内存中的地址。
    
5. **跳转执行**：跳转到内核的入口地址，将系统的控制权永久移交给内核。
主要关注设备树
##### 设备树
一种描述硬件系统组件和数据结构的语言。它是一个与平台无关的、树状结构的数据文件，用于将系统的硬件配置信息从 Linux 内核源码中分离出来。Bootloader 将其传递给内核，内核通过解析它来动态识别系统中的硬件，并加载对应的驱动程序。
主要用于实现**驱动代码与硬件配置的分离**，同一份内核与驱动搭配不同的设备树就可以支持不同的硬件平台。
设备树源文件扩展名为 **`.dts`** 或 `.dtsi`（包含文件），编译后生成 **`.dtb`** 二进制文件。
设备树配置错误，会导致内核无法识别硬件，当系统无法启动时，通过串口查看Bootloader和内核的打印信息，可以初步判断问题是出在硬件初始化阶段还是内核加载阶段。
设备树的核心结构是 **“节点”** 和 **“属性”，
节点描述一个总线或设备
属性描述该设备的特性，如地址、中断号、兼容性字符串。
可以使用dtc命令，设备树编译器，可以将 .dtb 反编译成可读的 .dts。
#### Kernel：Linux内核


#### Rootfs：Root files system 根目录系统
#### Application：应用层
## 命令

### pushd和popd
用于记住多个目录位置并在他们之间切换
pushd 其他目录把当前目录压入栈并且跳转到“其他目录”
dirs查看当前目录栈，popd调回到目录栈里上个目录
### file
查看文件信息，文件的类型等
### scp
 **Secure Copy** 用于在本地和远程计算机之间安全地传输文件。它基于 SSH 协议，因此提供了与 SSH 相同的安全性和身份验证方式。
 

![[Linux常见命令.png]]

##### 查看进程
当前终端关联的
ps
所有运行中的
ps aux
ps -ef
- **R (Running)**：运行中或可运行
    
- **S (Sleeping)**：睡眠状态（可中断）
    
- **D (Disk Sleep)**：不可中断睡眠（通常在进行IO）
    
- **T (Stopped)**：暂停状态
    
- **Z (Zombie)**：僵尸进程（已终止但未被父进程回收）
    
- **X (Dead)**：完全死亡（瞬间状态，很难看到）
pstree（树状结构显示进程关系）结尾加-p就是带PID

