
## 问题描述 日志轮换


不知道怎么轮换日志文件，具体点就是不知道怎么改文件名，怎么改变fopen的路径，有没有什么变量可以储存路径的？字符串就可以了吧。


### 报错：



### 思路阻塞


### 当前思路


可以定义个专用的日志文件链表，5个节点然后是路径，文件大小，指针域
定义个数组存储5个文件路径就可以了吧，确实可以，文件路径就是个字符串而已

### 解决办法
不需要直接把添加日志节点的函数里的路径改变，只在轮换函数里对当前的路径进行更新备份，然后每次删除最旧文件，即使失败也没事，然后是把每个文件重命名