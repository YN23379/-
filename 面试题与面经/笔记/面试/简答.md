## 自我介绍
1. 基本信息（20秒）：学校、专业、求职意向
2. 项目亮点（60秒）：重点讲1-2个项目的技术深度
3. 学习能力（20秒）：举例快速学习新技术的经历
4. 职业目标（20秒）：与公司业务相关的个人规划

## 非技术问题
### 优缺点
兴趣？
有意识的记录和总结，习惯在学习的过程通过Obisidian记录总结学到的知识和注意事项
自我审视

### 喜欢的工作和原因，选择工作的因素，怎么看待待遇和工作条件

选择工作大部分人都是“钱多事少离家近”，对我当下而言第一个的比重相对是最大的，我
待遇和工作条件主要是两个方面考虑，


### 期望薪资是多少，为什么是这么多？


### 职业规划


### 课设怎么样？


### 自我评价，几个词语形容一下，为什么这么评价？


### 大学里有什么成就？最值得骄傲的事是什么？



### 在学校的成绩排名？获得过什么奖项？

### 成绩低是为什么？为什么没参加比赛？

### 有参加什么社团吗？

### 你觉得别人对你的最多的评价是什么？说三个词？

### 上学期间有什么让你特别印象深刻的事么？

### 对行业和公司有什么了解？

### 现在有几个offer，几次面试，投的多吗？

### 课程学习有难度吗？哪个科目学的比较好？多少分？

### 觉得嵌入式软件的日常工作是做什么的

 2.为什么选择我们公司？ 3.你是0经验，为什么觉得你能够胜任这份工作？ 4.为什么觉得你能胜任这份工作？ 5.你怎么看待加班？ 6.用三个词描述一下你的优缺点 7.谈谈你的职业规划

优缺点：有较强的快速学习能力，
上手较快，执行力较强，沉稳负责，
追求周全，经验不足

## 技术相关

### 项目里遇到的有什么困难？


### 项目做了多久


### 一、C 语言基础 & 语言特性

1. **说一下 C 程序在内存中的大致布局？各段（text / rodata / data / bss / heap / stack）放什么？**
    
2. **局部变量、全局变量、静态局部变量、静态全局变量分别存在哪一段？生命周期和可见性有什么区别？**
    
3. **`const`、`static`、`extern`、`volatile` 分别解决什么问题？举 1~2 个你项目里的实际使用场景。**
    
4. **讲讲你对“未定义行为”的理解？平时写 C 代码怎么规避 UB？举两个典型例子。**
    
5. **`struct` 对齐规则大致怎么样？如果要减少结构体占用空间，你会怎么做？会有什么 trade off？**
    
6. **位段（bit-field）有什么用？在哪些嵌入式场景你会用它，在哪些场景你反而会避免用？**
    
7. **`memcpy` 和 `memmove` 的区别？如果让你自己实现一个 `memmove`，需要特别注意什么？**
    
8. **`strcpy` / `strncpy` / `strcat` / `snprintf` 这些字符串函数在安全性上的差别？你实际项目里倾向用哪种？**
    
9. **`malloc(1)` 通常实际会分配多少？为什么会“多给”？和内存对齐、管理开销有什么关系？**
    
10. **写一个简单的内存池或对象池的大致设计思路，和直接 `malloc/free` 比，有什么优缺点？**
    
11. **函数指针的语法你熟吗？能写出一个：接收函数指针作为参数的排序函数原型吗？（类似 `qsort`）**
    
12. **可变参数函数（`printf`）的实现大致依赖什么机制？你自己会在工程里写可变参数函数吗，注意点是什么？**

---

### 二、数据结构与算法（偏工程 & 嵌入式）

1. **单链表常见操作：反转、寻找倒数第 k 个、判断是否有环，你会用什么思路写？时间空间复杂度？**
    
2. **栈和队列在嵌入式系统里一般用来干嘛？举一个你项目里可以用栈/队列建模的场景。**
    
3. **环形缓冲区（循环队列）是什么？在串口 / 网络收发里怎么用？如何判断“满/空”？**
    
4. **二叉树的前序 / 中序 / 后序 / 层序遍历分别一般用来干什么？有没有写过非递归版本？大致思路？**
    
5. **常见排序算法里，你实际工程里会选哪几个？为什么？说一下快排和归并的优缺点。**
    
6. **讲一个你做过的动态规划题，说明：状态定义、转移方程、为什么这样定义。**
    
7. **哈希表在嵌入式上的使用需要注意什么？比如内存限制、冲突处理方式、实时性要求。**
    
8. **说说时间复杂度 O(1), O(log n), O(n), O(n log n) 各自你能举出哪些典型例子（尽量来自你实际代码）。**
    

---

### 三、Linux / 操作系统基础（用户态视角）

1. **进程和线程的区别？在 Linux 上它们到底是怎么实现的（比如 `clone`）？**
    
2. **进程的虚拟地址空间大致长什么样？（从低地址到高地址）堆、栈的增长方向呢？**
    
3. **为什么需要虚拟内存？页表、多级页表、TLB 大致是怎么配合工作的？**
    
4. **用户态调用系统调用（比如 `read`、`write`）的完整路径大概如何？系统调用号是怎么用的？**
    
5. **说说“页缺失（缺页中断）”是什么？在什么情况下会触发？对性能有什么影响？**
    
6. **多线程程序里，什么情况下会出现死锁？你如何在设计阶段尽量避免死锁？**
    
7. **进程间通信方式你知道哪些？各自适用场景、优缺点怎么对比？**
    
8. **`clone`、`fork`、`pthread_create` 的关系？Linux 内核里怎么区分“进程”和“线程”？**
    

---

### 四、Linux 系统编程（文件 I/O / 多线程 / 定时 / IPC）
4.1 文件 I/O

1. **`open` / `read` / `write` / `lseek` 和 `fopen` / `fread` / `fwrite` 的本质区别？缓冲上有什么差别？**
    
2. **阻塞 I/O、非阻塞 I/O、I/O 多路复用（`select/poll/epoll`）各适合什么场景？**
    
3. **`read` 返回值为 0、-1 分别代表什么？在网络编程和文件 I/O 中语义有何不同？**
    
4. **怎么实现一个简单的日志模块，支持：按大小切分、追加写、进程崩溃后不丢太多日志？**
    

4.2 多线程 / 同步

5. **`pthread_create`、`pthread_join` 的典型用法？为什么建议在合适的时候 `join`？**
    
6. **互斥锁（mutex）、读写锁（rwlock）、条件变量（cond）分别适合什么场景？举例。**
    
7. **自旋锁 vs 互斥锁？在 Linux 用户态下一般用哪种？为什么？**
    
8. **如果多个线程都要往同一个文件写日志，你会怎么设计避免日志交错 / 丢失？**
    

4.3 定时 / 信号

9. **Linux 用户态有什么方式做定时任务？（`alarm`、`setitimer`、`timerfd` 等）你会选哪个？为什么？**
    
10. **信号是异步到达的，为什么说“信号处理函数里能做的事情非常有限”？哪些函数是“异步信号安全”的？**
    
11. **`SIGINT`、`SIGTERM`、`SIGKILL` 区别？你在服务 / 守护进程里会如何设计退出流程？**
 4.4 IPC / `mmap`

12. **`mmap` 的典型使用场景有哪些？参数里 `PROT_*`、`MAP_*` 常用组合怎么选？**
    
13. **匿名映射（`MAP_ANONYMOUS`）和文件映射有何不同？在大块内存分配上，它和 `malloc` 有什么关系？**
    

---

### 五、网络基础 & Socket 编程（偏嵌入式）

1. **简要说一下 OSI 七层模型 & TCP/IP 四层模型，每一层在你写代码时分别会接触到哪些东西？**
    
2. **TCP 和 UDP 的区别，从：是否连接、可靠性、顺序、头部开销、典型应用场景 这几个角度说。**
    
3. **TCP 三次握手、四次挥手的过程，尽量画出 seq/ack 的变化，并解释为什么握手需要三次、挥手需要四次。**
    
4. **`TIME_WAIT` 状态是什么？为什么需要存在？在高并发短连接服务器中会带来什么问题？如何缓解？**
    
5. **客户端 / 服务器 TCP Socket 的典型调用流程，从 `socket` 到 `close`，按顺序说出每一步的作用。**
    
6. **`EAGAIN` / `EWOULDBLOCK` 什么时候会出现？如果你的 `recv` 经常返回 `EAGAIN`，你如何处理？**
    
7. **什么时候会收到 `SIGPIPE`？你会如何在程序里避免被 `SIGPIPE` 直接干掉？**
    
8. **在局域网内，两台设备通信时 ARP 的作用是什么？大致工作流程？网关在其中扮演什么角色？**
    
9. **子网掩码 / 网关 / DNS 这些配置错了，各自会导致什么表现？你怎么排查？**
    

---

### 六、嵌入式 / MCU / RTOS / 嵌入式 Linux

1. **裸机（无 OS）、RTOS（如 uC/OS、FreeRTOS）、嵌入式 Linux 的差别，你会从哪些维度来比较？（资源、实时性、开发体验、生态）**
    
2. **简单讲讲中断的基本概念：中断向量表、中断优先级、中断上下文和任务上下文的差别。**
    
3. **在 MCU 上操作寄存器通常有哪几种方式？直接地址、宏定义、HAL 库，各自优缺点？**
    
4. **PWM 的本质是什么？如何通过 PWM 控制电机转速或 LED 亮度？频率和占空比各自影响什么？**
    
5. **在 RTOS（如 uC/OS）里，任务、调度、时间片大致是怎么工作的？任务延时和阻塞有何区别？**
    
6. **RTOS 里的信号量、互斥量、消息队列这种 IPC，和 Linux 用户态线程同步原语相比，有什么不同点？**
    
7. **嵌入式系统里为什么很多时候不用 Swap？内存耗尽时一般会发生什么？（比如 Linux 上的 OOM killer）**
    
8. **简单说一下一个嵌入式 Linux 设备，从上电到你的应用程序开始运行，经历了哪些阶段？（Bootloader -> Kernel -> 根文件系统 -> init -> 应用）**
    
9. **根文件系统里 `/bin`、`/sbin`、`/etc`、`/dev`、`/proc`、`/sys`、`/usr`、`/var` 这些目录各干嘛的？你在调试设备时最常看的是哪几个？**
    
10. **如果让你裁剪一个用于嵌入式设备的根文件系统，你会优先保留什么、裁掉什么？依据是什么？**
    

---

### 七、工具链 / 交叉编译 / 调试

1. **交叉编译器和本地编译器有什么区别？`arm-none-eabi-gcc` 这种命名各部分代表什么？**
    
2. **简单讲一下从 `.c` 到可执行文件 ELF 的几个阶段：预处理 / 编译 / 汇编 / 链接，每一步大致做什么。**
    
3. **静态链接（`.a`）和动态链接（`.so`）的优缺点？在嵌入式场景你更常选哪种？为什么？**
    
4. **`readelf` / `objdump` 这些工具你用来干过什么？举一个你实际排查问题时用它们的例子。**
    
5. **你会用哪些方法在嵌入式 Linux 设备上调试程序？（gdb / 日志 / `strace` / `perf` / 自制 debug log 等）**
    
6. **如果程序一启动就崩溃，你没有 GUI，只能用串口 / SSH，你有哪些步骤来定位问题？**
    

---

### 八、项目深挖问题
8.1 系统监控器（Linux 上的多模块项目）

1. **先用 2~3 句话概括这个系统监控器的功能和整体架构。你负责的部分具体是什么？**
    
2. **数据采集模块是如何从 `/proc` 或系统接口获取 CPU / 内存 / 网络等指标的？刷新间隔如何控制？**
    
3. **监控器内部模块之间如何解耦？是用函数调用、消息队列、事件循环，还是别的方式？为什么这么设计？**
    
4. **日志模块是怎么实现的？有没有考虑过日志文件过大、磁盘写入压力、程序崩溃时的日志丢失问题？**
    
5. **在并发 / 多线程上，这个项目里遇到过哪些具体问题？比如竞态、死锁、性能瓶颈，你是怎么解决的？**
    
6. **如果让你把这个监控器“移植”到嵌入式 Linux 设备上，有什么地方必须修改或裁剪？**
    
8.2 OpenHarmony / 智慧安防小项目

7. **整体数据流是怎样的？从传感器采样到通过 MQTT 发到云端，中间经过了哪些步骤？**
    
8. **MQTT 的 QoS 你了解多少？你的实现里用了哪个等级？为什么这么选？**
    
9. **cJSON 是怎么用的？在内存有限的设备上，你如何避免频繁 malloc / free 带来的碎片问题？**
    
10. **你在这个项目里具体遇到过最难搞的 bug 是什么？怎么排查、怎么解决的？**
    
8.3 STM32 电磁循迹车 / 其他 MCU 项目

11. **循迹车的控制闭环是怎么搭起来的？传感器输入是什么，控制输出是什么，用的是什么控制策略（简单阈值 / PID）？**
    
12. **电机驱动那块，H 桥是怎么接的？正反转 / 刹车分别怎么控制？**
    
13. **在这个项目里，你是怎么组织中断、主循环、定时任务的？有没有遇到“卡死 / 抖动 / 丢步”的情况？**
    

---

### 九、通用 & 行为面试问题

1. **为什么选嵌入式 Linux 方向？和单纯搞应用开发、纯 MCU 方向相比，你最看重什么？**
    
2. **你如何保持自己的学习节奏？比如现在同时要刷题、看书、做项目，你是怎么安排时间的？**
    
3. **说一件你之前在项目里做错的事情（设计 / 实现 / 调试），你是怎么发现、怎么纠正、从中学到了什么？**
    
4. **如果入职后被安排去做一块你完全没碰过的东西（比如某种总线协议 / 某款 RTOS），你会怎么上手？给个 1 周和 1 个月的学习计划大致思路。**