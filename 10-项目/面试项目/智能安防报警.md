---
type: 项目档案
scope: 面试项目
doc_type: 未分类
status: 已整理
evidence: 实机验证
tags: [协议, 编译构建, 进程与线程, 体系结构]
updated: 2026-09-17
---

老师要求：
智能安防报警系统主要实现遭遇窃贼时报警的功能。当家中发生非正常紧急情况（有人接近）时，通过一系列传感器的感应，及时将测量数据反馈给设备端，判断事故后立即进行声光报警。

具体任务分设备端和小程序端

1.设备端，功能包括：
①NFC配网功能；
②利用红外传感器检测是否有人闯入，有人并超时则判断为异常；
③发布和订阅 MQTT 的消息；
④保存红外传感器的阈值信息到 Hi3861 模块中的 Flash 存储中；
⑤屏幕显示传感器及控制等关键信息。

2.开发微信小程序，功能包括：①将配网信息数据写入 NFC 标签中；②手动控制蜂鸣器的开关；③设置红外传感器的阈值范围；④开启自动控制功能；⑤显示当前红外传感器的数据和蜂鸣器当前的状态。

3.其他自由发挥内容。
# 基于 OpenHarmony 的智慧安防报警系统（IoT 边缘应用）


解决了内存泄漏问题，
**`cJSON_PrintUnformatted(json_root)`**： 这一行，cJSON库在**堆内存上动态分配**了一块空间，并将格式化后的JSON字符串的指针`payload`返回
使用下面的free
```
 char *payload = cJSON_PrintUnformatted(json_root);
    if (payload != NULL) {
        // 发布消息
        MQTTClient_pub(publish_topic, payload, strlen(payload));
        cJSON_free(payload); // 或者使用 free(payload); 取决于cJSON的配置
    }
    cJSON_Delete(json_root);
```

项目本质上是一个**物联网边缘设备控制系统（智能安防报警系统）**。它运行在嵌入式 Linux / OpenHarmony 平台上，完成了从传感器数据采集 → 状态判定 → MQTT 通信 → 云端同步 → 显示与报警 的完整闭环。

**NFC（NDEF）→ Wi-Fi（Hi3861 内置 2.4 GHz）→ MQTT（Paho/lwIP）→ 云/小程序**；
**AP3216C@I²C 采样 → 状态机判定 → AW2013@I²C 控灯、蜂鸣器 GPIO → SSD1306@I²C 显示**。  
**开发**：DevEco（集成 hb/GN+Ninja）+ riscv32 交叉链；**OS**：OpenHarmony LiteOS-M。

## 开发环境搭建
### **OpenHarmony环境**：
Hi3861运行的是运行 **OpenHarmony 轻量系统（基于 LiteOS-M 内核）**（华为推出的开源**分布式操作系统**），而不是纯Linux。因此不能用普通的`gcc`来编译，必须使用
OpenHarmony整体遵从分层设计，从下向上依次为：内核层、系统服务层、框架层和应用层（详见OpenHarmony技术架构）。系统功能按照“系统 > 子系统 > 部件”逐级展开，在多设备部署场景下，支持根据实际需求裁剪某些非必要的子系统或部件。子系统是一个逻辑概念，它具体由对应的部件构成。


**DevEco Device Tool**：这是华为官方提供的**一站式开发IDE**。它内部集成了OpenHarmony的编译工具链、代码编辑、编译、烧录、调试等一系列功能。**使用它是为了简化配置，避免手动配置极其复杂的环境变量和编译脚本。**
OpenHarmony专用的**编译工具链**
**RISC-V 32 位（RV32IMAC）** 交叉工具链



--- 
### 硬件
#### 开发板与系统
开发板是Hi3861V100，板上带 I²C 外设：AP3216C（红外/接近/环境光）、AW2013（RGB LED 驱动）、SSD1306（OLED），以及 NFC 模块做配网。
芯片是Hi3861v100，是一颗以 **MCU** 为核心的 **Wi-Fi SoC**，属于资源受限的轻量级设备。
**系统内核**：OpenHarmony 的轻量系统--LiteOS-M
（OpenHarmony 不绑定单一内核。它是一个**操作系统框架**，可以根据设备资源，选择最合适的内核，比如LiteOS-M，LiteOS-A,Linux）
#### 关键规格

- **CPU**：32位 RISC-V 架构或 ARM Cortex-M 架构的单核处理器。（注：早期版本多为ARM-M，后续有RISC-V版本）
- **无线**：支持 2.4GHz Wi-Fi (802.11b/g/n)
- **内存**：集成 SRAM，容量在几百KB级别。
- **存储**：集成 Flash，容量在MB级别。
- **外设**：提供 GPIO、I2C、I2S、PWM、UART 等接口，用于连接传感器和执行器。

IDE/构建链：**DevEco Device Tool**：面向 OpenHarmony 设备的一站式 IDE（VS Code 插件），集成代码编辑、编译、烧录、调试，支持 Hi3861V100 等开发板。
**hb 构建工具**：OpenHarmony 的命令行构建入口，本质用 **GN+Ninja** 做底层构建；`hb set` 选择产品/内核/板型，`hb build` 出镜像。

- GN：Generate Ninja的缩写，高效的构建工具，用于产生Ninja文件。
- Ninja：Ninja是快速的执行器，支持并行与增量编译。
- hb：OpenHarmony的命令行工具，用来执行编译命令。
[[编译构建工具#二、构建系统演化#GN与Ninja]]

DevEco 省去手配工具链，一键编译烧录；hb 是官方入口，上层调用 GN/Ninja，产品化配置清晰，适合多板型多组件组织。
#### AP3216C
是一颗三合一（环境光/红外/接近） I²C 数字传感器，提供 ALS（环境光）、IR、Proximity，走 I²C，寄存器配置简单，适合做人靠近检测。[知乎专栏](https://zhuanlan.zhihu.com/p/677091825?utm_source=chatgpt.com)
只用接近通道（PS），阈值（`ps_threshold`）+ 超时（`timeout_threshold`）配合状态机判断“有人且持续超时 → 报警”。
    
- 注意点：
    
    - 上电初始化顺序：复位→开通道→设增益/积分时间（影响噪声与延迟）。
        
    - 采样频率与去抖：PS 读值抖动，用移动平均或时间窗确认“连续 N 次超阈”再触发。
        
    - 在强光/反射面前如何避免误报？——PS 饱和时做软钳位+短时降低积分时间；再辅以阈值自适应或环境校准。


#### AW2013
RGB LED 恒流**驱动**
是AWINIC 出的 3 通道 RGB LED 驱动，I²C 配置，带电流与 PWM 控制，常用于状态灯/告警灯。[51CTO](https://ost.51cto.com/posts/7898?utm_source=chatgpt.com)
红灯通道显示三种状态：**常亮**（手动）、**闪烁**（自动告警）、**熄灭**（正常）。

- 注意点：
    
    - 典型步骤：`enable → set current → set PWM/呼吸`，先开稳流再设占空比。
        
    - 冲突仲裁：人为常亮优先级最高，其次是自动闪烁；避免“手动长鸣时误闪”的状态竞争（我们后来做了清晰的优先级+去抖）。
        
    - 为什么”按一次长鸣“灯会闪一下？——状态边沿导致分支条件瞬时满足，需加**状态去抖/延迟仲裁**。
        

#### SSD1306
0.96" 128×64 OLED 控制器
Solomon Systech 的常见 OLED 控制器，I²C/SPI 皆可，页寻址显示。[百度开发者中心](https://developer.baidu.com/article/details/3275590?utm_source=chatgpt.com)
显示**是否有人、是否报警、PS 实时值/阈值**与模式。
    
- 注意点：
    
    - 显示缓存（framebuffer）→ 分页刷屏，注意**缓冲清零**和**越界写**会导致“半行残影/错色”。
        
    - `printf`/`sprintf` 的类型匹配（比如 `difftime` 是 double，别用 `%d`）和长度控制，避免随机字符。
        
    - 为何会出现“上一帧字符残留”？——没清缓冲或写入长度小于上次，需覆盖/清空对应区域。
        

#### NFC 模块
用于「NFC 配网 / NDEF」
- **是板载一颗 I²C 接口的 NFC 动态标签/读写器**（课程板通常集成 NDEF 支持），用手机写入 Wi-Fi SSID/密码；设备上电读取 NDEF 完成配网。华清的板卡说明明确“带 NFC RFID 通信芯片”。[HarmonyOS 设备开发官网](https://device.harmonyos.com/cn/develop/ide?utm_source=chatgpt.com)
- 
**联网路径**：NFC 取 SSID/密码 → Wi-Fi 上网 → MQTT（Paho）建连 → 订阅/发布；
- 注意点：
    
    - **数据留存**：这类 NFC 动态标签自带非易失存储，掉电不丢；实际配网流程是**手机→NFC 标签（NDEF）→ 板端 I²C 读取 → Wi-Fi 连接**。
    - 生产注意：NDEF 分区、容量限制、写保护/密码、防止把敏感信息回传到云端日志。
    - 手机贴一下就能连网的关键是什么？——答：**NDEF 数据模型**与设备侧对 NDEF 解析+Wi-Fi 驱动的衔接。



--- 




## 数据采集


通过对传感器的采集之后在函数里进行判断

通过状态机实现利用红外传感器检测是否有人闯入，有人并超时则判断为异常；

### I2C通信
- I2C是一种**同步、半双工**的两线式串行总线，由**串行数据线（SDA）** 和**串行时钟线（SCL）** 构成。Hi3861作为**主机（Master）**，通过I2C总线连接了AP3216C（接近/光感传感器）、AW2013（LED驱动）和SSD1306（OLED屏）等**从设备（Slave）**。
    
- **关键步骤**：
    
    1. **初始化I2C控制器**，配置GPIO引脚（SCL, SDA）和时钟频率[](https://docs.espressif.com/projects/esp-techpedia/zh_CN/latest/esp-friends/get-started/case-study/peripherals-examples/i2c-examples/i2c-basic-example.html)。
    2. 对于像AP3216C这样的传感器，通过**写寄存器**来配置其工作模式[](https://docs.espressif.com/projects/esp-techpedia/zh_CN/latest/esp-friends/get-started/case-study/peripherals-examples/i2c-examples/i2c-basic-example.html)。
    3. 通过**读寄存器**来获取传感器的数据[](https://docs.espressif.com/projects/esp-techpedia/zh_CN/latest/esp-friends/get-started/case-study/peripherals-examples/i2c-examples/i2c-basic-example.html)。
- **遇到问题怎么办**：可以提及在实际开发中，如果遇到I2C通信失败，先**检查硬件连接**（线缆、上拉电阻）、**用逻辑分析仪抓取波形**分析时序，并**确认从设备地址和寄存器地址是否正确**。

## 项目架构
这是一个**端云一体**的物联网项目，最终实现了从感知、决策、控制到云端管理的完整闭环。这个项目主要工作被我分为设备端，小程序端和云端，我的角色是项目负责人和设备端开发，设备端是在开发板上进行数据采集和处理，进行相应的控制，在OLED屏上进行显示，再把数据通过MQTT发送到云端，同时接收来自华为云或者小程序的命令进行控制
小程序端是展示的核心之一，通过构建微信小程序的页面和配置进行数据的接收和展示，同时也是下发命令的平台，云端就是负责数据的转发和维护通信的稳定，当然也可以直接进行命令下发。

### 构建流程
[[编译构建工具#GN与Ninja]]
1. **定义模块 BUILD.gn：在`BUILD.gn`中声明源码、编译选项及**依赖的其他模块，这是构建系统工作的输入和依据。
2. **配置产品 (hb set)**：选择目标产品与开发板，决定后续使用的**内核、驱动组态和交叉编译工具链**。
3. **生成与编译 (hb build)**：
    - **生成**：GN工具**解析所有模块的`BUILD.gn`**，理清完整的依赖树，生成高效的Ninja构建脚本。
    - **编译**：Ninja驱动**Clang交叉编译工具链**，按脚本将源码编译、链接为`target.elf`等固件文件。
4. **烧录运行**：通过J-Link或串口将最终固件烧录至Hi3861硬件上运行。

示例模块：
mqtt发送，mqtt接收，传感器采集，显示屏输出，主函数

主函数：设备启动 → 外设/NFC 初始化 →（NFC）获取 WiFi 配置 → 连接 WiFi → 连接 MQTT（server → client → subscribe）→ 创建三条任务并发运行


--- 
#### 状态机实现
通过AP3216C接近传感器检测人体接近，用**三态状态机**判断是否报警，根据状态控制声光设备和OLED显示
通过全局结构体进行数据传输，警铃状态，传感器值，设定的阈值，自动模式状态，nvFLASH，

先枚举定义状态机状态，在采集函数里周期读取AP3216C传感器数据，根据这个值和设定的阈值进行状态改变
在自动模式下，用状态机实现非阻塞的超时判断，开始的时候是正常状态，有人则进入追踪状态，也就是传感器的值超过了设定的接近阈值，用时间戳通过difftime函数记录时间差，持续时间超过报警阈值则进入报警状态，报警状态会根据设定的模式进行声光报警，同时也提供警铃长鸣，传感器的值和状态机状态会实时显示到OLED屏上

蜂鸣器控制：set_buzzer（true)
OLED屏显示：SSD1306_ShowStr（0,0(unsigned char*)buffer，TEXT_SIZE_16）
LED控制：AW2013_Control_RGB（0,0,0）

状态机的状态更新都需要清空缓冲区，也就是输出到OLED屏幕的部分，不然可能会造成显示出错，半行显示

#### MQTT收发
MQTT是应用层协议，在Socket基础上封装了订阅发布、QoS质量等级、遗嘱消息等物联网专用特性。直接使用MQTT可以避免在应用层重复实现连接管理、消息路由等复杂逻辑，提高开发效率。
[[网络编程#Socket网编]]

##### 华为云
在华为云物联网平台中，我们首先创建IoTDA实例，然后在其中创建"智能安防报警器"产品，该产品下定义了base和control两个服务。base服务专门用于设备向云端上报状态数据，这些数据会同步到微信小程序显示；control服务则用于接收从华为云控制台或微信小程序下发的控制命令，实现双向通信。

cJSON是一个**轻量级**的C语言JSON解析库，非常适合在**资源受限的嵌入式环境**中使用[](https://bbs.huaweicloud.com/blogs/279988)。它核心的结构体是 `cJSON`，提供了`cJSON_Parse()`、`cJSON_GetObjectItem()`等API来**解析和构建**JSON数据[CJson](https://bbs.huaweicloud.com/blogs/279988)。
[[网络编程#`cJSON`]]
- C语言没有内置的JSON处理能力
    
- 需要将内存中的结构体数据**序列化**成JSON字符串才能发送
    
- 也需要将接收到的JSON字符串**反序列化**成结构体数据

| 函数                        | 作用          | 代码示例                                      | 内存管理     |
| ------------------------- | ----------- | ----------------------------------------- | -------- |
| **cJSON_Parse()**         | JSON字符串→对象树 | `cJSON *root = cJSON_Parse(payload)`      | 需要Delete |
| **cJSON_GetObjectItem()** | 获取对象字段      | `cJSON_GetObjectItem(root, "service_id")` | 自动管理     |
| **cJSON_Delete()**        | 释放整个对象树     | `cJSON_Delete(root)`                      | 必须调用     |

| json_command_name结构体成员 | 作用     |                     |          |
| ---------------------- | ------ | ------------------- | -------- |
| **valuestring**        | 读取字符串值 | `item->valuestring` | 只读，勿free |
| **valueint**           | 读取整数值  | `item->valueint`    | 只读       |

**Paho Embedded-C** 是 Eclipse 基金会旗下的一款开源的、专为**资源受限的嵌入式设备**设计的 **MQTT 客户端库**。
- **轻量级**：代码体积小，内存占用低，非常适合 Hi3861 这种内存只有几百KB的MCU。
- **可配置**：可以裁剪掉不需要的功能（比如 QoS 2），进一步节省资源。
- **嵌入式友好**：它与底层网络栈（如 lwIP）和操作系统（如 LiteOS-M）有良好的适配接口。

| 函数                         | 作用        | 项目中的使用     |
| -------------------------- | --------- | ---------- |
| `MQTTClient_connect()`     | 连接MQTT服务器 | 设备启动时调用    |
| `MQTTClient_subscribe()`   | 订阅主题      | 接收云端命令     |
| `MQTTClient_pub()`         | 发布消息      | 发送传感器数据/响应 |
| `MQTTClient_yield()`       | 处理网络流量    | 保持连接活跃     |
| `MQTTClient_setCallback()` | 设置消息回调    | 指定消息处理函数   |
|                            |           |            |

##### 接收
**当云端通过MQTT下发控制命令时，会通过接收模块提取topic中的request_id用于请求追踪，解析JSON payload获取具体指令（如开关蜂鸣器、设置传感器阈值等），更新设备状态后立即通过MQTT返回执行结果，确保可靠响应。**
，通过回调函数实现MQTT数据的接收，这一部分主要实现命令解析、状态更新和响应反馈。当云端下发控制命令时，解析JSON数据更新设备状态，并立即返回执行结果给云端。

订阅 接收控制命令的主题，实现MQTT消息回调，接收并验证云端命令：接收到命令消息之后先和本地的设备id，命令名称进行校验，成功才继续执行
用cJSON库解析JSON数据：一条消息是一次命令，通过strncmp()比较得到命令名称然后解析命令内容，一般就是自动模式的ON和OFF，阈值大小，根据命令设置对应的参数到处理函数里
发布 成功接收到控制命令后的主题，构建响应消息返回云端：确认topic发送的request_id，保证请求响应匹配，1成功，0失败

用回调是因为MQTT消息到达时间不确定，回调让系统在消息到达时立即处理，而不是不断轮询浪费资源。这是典型的事件驱动架构。

##### 发送
发布 设备属性数据的主题，MQTT发送模块负责将设备状态数据封装为JSON格式并发布到云端。通过cJSON库动态构建包含传感器数值、报警状态、设备模式等关键信息的JSON对象，使用`cJSON_PrintUnformatted`生成payload后通过MQTT发布，并确保在发送完成后正确释放所有动态分配的内存资源。

创建根JSON对象，首先挂载一个services数组以符合物联网平台的多服务数据模型；接着在数组中构建具体的service对象，通过service_id标识服务类型为'base'；然后在该service下创建properties对象作为所有设备属性的容器；最后将传感器数值、开关状态、工作模式等数据逐一注入properties中，完成数据组装后序列化为紧凑JSON格式通过MQTT发出,最后释放所有JSON的资源，Delete根对象，置空创建的那些子对象

先创建根对象→添加services数组→在数组中创建service对象→设置service_id为"base"→添加properties对象→最后将所有数据字段添加到properties中。

| 函数                             | 作用        | 参数说明          | 内存管理           |
| ------------------------------ | --------- | ------------- | -------------- |
| **cJSON_CreateObject()**       | 创建JSON对象  | 无             | 需要cJSON_Delete |
| **cJSON_CreateArray()**        | 创建JSON数组  | 无             | 需要cJSON_Delete |
| **cJSON_AddItemToObject()**    | 添加字段到对象   | 父对象, 键名, 值对象  | 自动管理           |
| **cJSON_AddStringToObject()**  | 添加字符串字段   | 父对象, 键名, 字符串值 | 自动管理           |
| **cJSON_AddNumberToObject()**  | 添加数值字段    | 父对象, 键名, 数值   | 自动管理           |
| **cJSON_PrintUnformatted()**   | 生成JSON字符串 | cJSON对象       | 需要cJSON_free   |
| **MQTTClient_pub()**           | 发布MQTT消息  | 主题, 载荷, 长度    | 网络操作           |
| **MQTTClient_connectServer()** | MQTT重连    | 服务器IP, 端口     | 阻塞式重连          |


#### 线程池
**线程池采用CMSIS-RTOS2 API构建，核心是定时器驱动+消息队列+单管理线程的架构：
硬件定时器作为生产者按预设间隔触发，通过消息队列将任务ID传递给唯一的管理线程，由它统一执行对应的任务回调函数。这种设计通过解耦触发与执行、复用单个工作线程，在保证三个周期性任务（MQTT收发、传感器采集）稳定运行的同时，最大程度降低了嵌入式系统的内存占用和线程切换开销。**

基于**CMSIS-RTOS2 API**实现具体代码，
生产者是定时器 (`osTimer`)，消费者是线程池管理线程 (`thread_pool_manager_task`)，缓冲区是消息队列 (`osMessageQueue`)
**流程**：三个硬件定时器按不同间隔触发 → 定时器回调向消息队列发送任务ID → 单工作线程从队列取出任务ID → 执行对应的任务函数。
初始化时创建消息队列和任务数组，然后注册任务，为每个周期性任务创建独立的硬件定时器
当定时器到期时，在中断上下文中快速向消息队列发送任务ID，之后工作线程会阻塞在消息队列上，直到收到任务ID后立即执行对应的任务函数，最后执行完毕返回等待状态，准备处理下一个任务

**同步机制**：通过消息队列自然同步，无需锁。定时器在中断上下文快速投递，工作线程在任务上下文执行，解耦且线程安全。

和传统POSIX线程池区别：主要区别在于**任务触发机制**。传统线程池处理的是外部提交的离散任务，而这个实现是**基于时间的周期性任务调度**。它更像是'定时任务执行器'，特别适合物联网设备的数据采集、心跳上报等场景。

虽然任务执行是串行的，但我们的MQTT通信和传感器采集都是非阻塞的
使用消息队列（不直接执行任务）是为了实现**解耦**和**流量控制**。定时器回调在中断上下文中执行需要尽快返回。通过消息队列可以将'触发'和'执行'分离，避免在中断中执行复杂逻辑，同时队列长度限制防止任务堆积。"

硬件定时器 (osTimer)：不是软件循环，而是**芯片内部的硬件定时器**。由硬件保证，比`sleep`或轮询精确得多。`osTimerPeriodic` 表示周期性触发，无需手动重启。   
时间到了就仅仅发送任务ID到消息队列，完成解耦，也就是将“时间触发”和“任务执行”完全分离。

#### FLASH
基于OpenHarmony NV模块的参数掉电存储功能，用于保存设备的关键配置参数。当用户通过小程序设置报警阈值、工作模式等参数时，这些数据会立即写入Flash，确保设备重启后所有设置都能保持，实现了'设置即保存，开机即生效'的用户体验。

定义一个结构体来组织存储数据，包括蜂鸣器状态、自动模式标志、灯光设置、传感器阈值和超时时间等关键参数。使用Hi3861提供的`hi_factory_nv_write`和`hi_factory_nv_read` API进行Flash的读写操作，并在每次写入后立即验证读取，确保数据完整性。

OpenHarmony的NV模块提供了封装好的安全读写接口，避免了直接操作Flash的复杂性，同时保证了在不同硬件平台上的一致性，是我们项目的最佳选择。


### 交叉编译


| 特性        | 香橙派H616 (Linux系统监控器)      | Hi3861 (智慧安防系统)             |
| --------- | ------------------------- | --------------------------- |
| **工具链名称** | **aarch64-linux-gnu-gcc** | **riscv32-unknown-elf-gcc** |
| **目标架构**  | ARM Cortex-A53 (64位)      | RISC-V (32位)                |
| **运行环境**  | 带Linux内核的完整操作系统           | 无OS或OpenHarmony LiteOS-M内核  |
| **编译方式**  | 在PC上编译，可执行文件拷贝到板子运行       | 在PC上编译，生成**固件**烧录到板子        |
| **链接库**   | 动态链接到板载Linux系统的标准C库       | 静态链接，所有库**编入固件**            |
- **`riscv32`**: 目标CPU是32位RISC-V架构（Hi3861芯片核心）。
    
- **`unknown`**: 代表没有特定的供应商。
    
- **`elf`**: 输出格式为ELF文件，用于生成最终的二进制固件。
- **顶层工具：`hb` (OpenHarmony Build)**
    
    - 你用的命令是 `hb set` 和 `hb build`。
        
- **底层系统：GN + Ninja**
    
    - `hb` 是一个入口，它调用 **GN (Generate Ninja)** 来配置和生成构建脚本。
        
    - GN再调用 **Ninja** 这个高效的构建工具，驱动`riscv32-unknown-elf-gcc`完成实际的编译和链接。


**为什么Hi3861不能像香橙派那样编译一个可执行文件直接运行？**

Hi3861可以看作一个**裸机环境**（或运行极轻量的内核），没有独立的文件系统和加载器。所以我们必须把应用程序和操作系统（LiteOS-M）内核、驱动程序**一起编译链接成一个单一的、可直接在硬件上运行的固件二进制文件**，然后通过烧录工具整个刷入Flash。




---

## 问题
1. **“消息队列长度设多少？丢任务怎么办？”**  
    **背一句**：
    
    > “队列长度 = 10，**溢出时 `osMessageQueuePut` 返回 `osErrorResource`**，直接统计溢出次数并上报云端，**1 h 内溢出 0 次**。”


### 状态竞争
手动长鸣时自动报警触发。通过明确的优先级规则解决了这个问题


"1.5行显示"和"颜色分割"说明：

- 数据写入了**显示缓冲区的相邻内存**
    
- 上半部黄色：写到了第一行缓冲区末尾
    
- 下半部蓝色：写到了第二行缓冲区开头
  
直接把difftime(current, last) 作为参数传到sprintf的格式化输出里，格式都是对的，对应的是%2d,但是显示的是-21？
`difftime()` 函数返回的是 **`double` 类型**（表示秒数，可能带小数），但你用 `%2d` 来格式化，`%d` 是用于 `int` 类型的。

**类型不匹配导致内存解释错误，所以显示奇怪的负数。**

**缓冲区没有初始化/清零**导致的显示残留。
1. `difftime(current_time, 0)` 计算的是从1970年到现在的时间差，结果是巨大的正数
    
2. **强制转换为 `int`**：巨大的 `double` 转 `int` 发生溢出，变成负数


## 简历面试
**智慧安防报警系统｜技术负责人｜OpenHarmony/Hi3861（C）**

- 在Hi3861上实现边缘端安防应用，完成**红外采集→状态机判定→声光报警→MQTT通信→OLED显示**的完整物联网闭环，并与微信小程序联动。
    
- **核心状态机设计**：独立设计并实现三态自动机（ALARM_OFF/TRIGGERED/ON）与阈值/超时逻辑，**采用非阻塞时间戳比对替代sleep**，确保系统低延迟与高响应性。
    
- **MQTT通信架构**：基于Paho Embedded-C实现通信链路，**定义主题与载荷规范**；修复`cJSON_PrintUnformatted`未配对释放导致的**堆内存泄漏**，保障系统长时稳定运行。
    
- **高并发任务调度**：将MQTT发送/接收重构为**线程池定时任务**，通过互斥锁与条件变量实现同步，**统一系统任务节拍**，有效控制线程数量与栈内存占用。
    
- **系统持久化**：利用OpenHarmony NV模块将关键配置（如报警阈值）保存至Flash，实现**掉电不丢失**，并在启动时自动加载。

- 关键文件：`sensor_collect_task.c`（采集/状态机）、`mqtt_send_task.c / mqtt_recv_task.c`（通信）、`thread_pool.c`（并发模型）、`smartSecurityDefense_demo.c`（启动流程）
    

> 关键词：C / OpenHarmony / RTOS任务 / MQTT / JSON(cJSON) / 状态机 / Flash持久化 / OLED / NFC

---
Paho Embedded-C 是一个轻量级的 MQTT 协议库，我们在 OpenHarmony 项目里用它来实现设备与 MQTT 服务器的连接、消息发布和订阅。


### 追问回答

- **Q1：为什么做线程池？**  
    A：多条永循环线程各自 `sleep` 的节拍漂移难控，栈占用也高。线程池用一个工作线程按“间隔时间”调度 MQTT 发送/接收/心跳，**统一时基 + 省内存**，也便于后续加任务（如重连退避）。
    
- **Q2：你怎么定位并修复 MQTT 上报崩溃？**  
    A：长跑后崩溃、堆增长 → 检查每个上报路径，发现 `cJSON_PrintUnformatted` 返回堆内存未 `cJSON_free`。补齐释放后复测稳定，并在异常路径加保护与日志。
    
- **Q3：状态机比 if-else 好在哪？**  
    A：把“检测/计时/报警”抽成 **状态与转换**，更清晰也更易加“去抖/最小驻留时间”。灯与警铃冲突用“**优先级表**”（手动常亮＞自动闪烁＞仅长鸣灭灯）解决。
    
- **Q4：NV 持久化怎么做的？**  
    A：定义含 `ps_threshold/timeout_threshold` 的 NV 结构体，启动时 `read→校验→回填运行变量`；命令设置后 `write` 写回。这样参数掉电不丢，链路“云命令→本地生效→落盘”闭环。
    
- **Q5：如果继续优化？**  
    A：MQTT 重连用 **指数退避**；上报加 **QoS1** 与小型发送缓冲；线程池支持**漂移校正**；把 NV 封成 `load/save` API 并统一错误码与日志级别。
- **“你的全局结构体是怎么管理的？考虑过并发访问吗？”**
    
    - **答**：“结构体包含了所有设备状态。在读写时，我通过简单的开关中断（或在Linux下可以用互斥锁）来保证关键数据的原子性，避免竞态条件。”
        
- **“MQTT断线了怎么办？”**
    
    - **答**：“Paho库有自动重连机制，我设置了合理的重连间隔。在生产环境中，我还会在本地缓存关键数据，待网络恢复后重新上报。”
        
- **“JSON数据格式是你自己设计的吗？怎么考虑的？”**
    
    - **答**：“是的。我参考了物联网平台的常见规范，设计了`deviceID`、`timestamp`、`sensorData`等字段，兼顾了可读性和传输效率。”
        

#### **2. C语言基础问题**

- **“`static`关键字有什么作用？”**
    
    - **答**：（记住三点）1. 隐藏：限制变量/函数作用域于本文件。2. 持久：使局部变量生命周期延长至程序结束。3. 初始化为0。
        
- **“`const int *p` 和 `int * const p` 有什么区别？”**
    
    - **答**：“前者是指向常量的指针，指针可以变，指向的值不能变；后者是常量指针，指针不能变，指向的值可以变。”
        
- **“什么是内存泄漏？怎么避免？”**
    
    - **答**：“动态申请的内存没有释放。我会通过Valgrind工具检测，并遵循`malloc/free`成对出现的编程规范。”
        

#### **3. Linux应用问题**

- **“进程和线程的区别？”**
    
    - **答**：“进程是资源分配的单位，有独立的地址空间；线程是CPU调度的单位，共享进程资源。线程切换开销更小，但需要处理同步问题。”
        
- **“用过哪些Linux调试工具？”**
    
    - **答**：“GDB用于调试段错误，Valgrind用于检查内存泄漏，`strace`用于跟踪系统调用，`ps/top`用于查看进程状态。”
--- 
### 总结版本

“我们用的是 **HiSpark Wi-Fi IoT（Hi3861V100）** 板，OpenHarmony 的 **LiteOS-M** 形态，用 **DevEco + hb（GN/Ninja）** 构建。硬件这边通过 **AP3216C** 做接近检测，**AW2013** 驱动 RGB 灯提示，**SSD1306** 做页面显示，配合 **NFC NDEF** 做一贴即配网。软件侧我负责 **MQTT 通信、采集状态机和任务并发**：用阈值+时间窗判定入侵，红灯做常亮/闪烁的优先级仲裁，MQTT 上报属性并支持阈值下发写到 Flash 防掉电。”

 这个项目是跑在 **Hi3861 + OpenHarmony** 的边缘端安防应用。我负责 **采集+状态机、MQTT 通信和并发模型**。系统从 AP3216C 周期采集接近值，进入三态状态机：进入→计时→超时触发报警，联动蜂鸣器/红灯，并通过 **MQTT** 上报到云端；小程序下发命令后设备解析 JSON 即时生效。  
工程上我把 MQTT send/recv 放进自建 **线程池** 做定时任务，替代多线程 while 循环，统一节拍、节省栈空间；修了 `cJSON_PrintUnformatted` 未释放导致的 **堆泄漏** 和 `difftime(double)` 用 `%d` 的类型错误，并处理了 OLED 缓冲残留。阈值/超时参数放到 **Flash(NV)** 持久化，启动加载、变更即写回。整体实现“采集→判定→声光→MQTT→小程序”的闭环并通过长时运行验证稳定。

“我负责的是一个基于Hi3861和华为云的物联网设备端项目。我的核心工作是**将硬件数据可靠地接入云端，并实现双向控制**。

1. **数据采集与整合**：我设计了一个全局状态结构体，统一管理来自不同传感器的数据，作为设备运行的单一数据源。
    
2. **云端通信桥梁**：我使用了Paho MQTT客户端库，并基于cJSON库，实现了设备状态数据与云端JSON协议之间的**双向转换和可靠传输**。我清晰地定义了`base`和`control`等主题，来区分上行数据和下行命令。
    
3. **业务逻辑实现**：我完成了从传感器数据采集、封装、上传，到接收云端指令、解析并更新设备阈值（如报警阈值）的**完整业务闭环**。
### 问答
1. **这块板是什么架构？为什么看到 RISC-V？**

> **答**：Hi3861V100 IoT SoC，OpenHarmony 的 Hi3861 方案配的是 **RISC-V 32（RV32IMAC）** 工具链与 LiteOS-M 运行时；我这套工程的编译参数里就有 `-march=rv32imac`，IDE 也是走 riscv32 交叉链。[Alldatasheet](https://www.alldatasheet.com/datasheet-pdf/view/1179026/ETC2/SSD1306.html?utm_source=chatgpt.com)

2. **OpenHarmony 的构建链路是怎样的？IDE 点“编译”发生了什么？**

> **答**：DevEco 调用 **hb**；hb 基于 **GN 生成 Ninja**，再用交叉编译器（`riscv32-unknown-elf-gcc`）编、链出固件；失败看 `build.log`，`target.elf` 缺失多半是前一阶段报错导致未产物。[CSDN博客](https://blog.csdn.net/gitblog_06698/article/details/142571786?utm_source=chatgpt.com)

3. **I²C 外设你怎么用？需要注意什么？**

> **答**：都走 I²C：AP3216C（ALS/PS/IR）、AW2013（RGB 驱动）、SSD1306（OLED）。我遵循**上电顺序**、**地址配置**与**读写时序**；遇到总线忙/抖动，用**超时+软复位**清状态；显示异常一般是 **GDDRAM 未清/缓冲区残留**（我遇到过，先清屏再写）。[中源液晶+2WatElectronics.com+2](https://sino-lcd.com/icDetails/SSD1306.html?utm_source=chatgpt.com)

4. **Wi-Fi/ MQTT 在这块板上怎么跑？**

> **答**：SoC 内置射频，系统用 OpenHarmony 的 Wi-Fi Service 建链；上层用 Paho Embedded-C 建 MQTT 客户端；我设计了 **主题命名**（如 `device/{id}/props`）、**QoS1** 上报、**心跳/重连**与 **内存释放（如 cJSON_print 的 payload 释放）**，避免内存泄漏。

5. **课程框架里封装很多，你到底做了什么？**

> **答**：我负责三件“应用层可衡量”的事：

- **采集与状态机**：把 AP3216C 的 PS 值做“有人/超时=报警”的状态机（`ALARM_OFF → TRIGGERED → ALARM_ON`），并把 **灯光/蜂鸣** 的“手动/自动/冲突优先级”明确化；
    
- **MQTT 通信**：属性上报 JSON、命令下行解析、**内存释放**与重连；
    
- **参数持久化**（按你现在进度）：PS 阈值/超时阈值与运行态同步 + Flash/NV 保存（调用厂商 `hi_factory_nv_read/write` 一类 API）。

6. **如果我拔电再上电，设备还记得阈值吗？**
    

> **答**：记得。阈值同步到 **NV/Flash**，启动先 `nv_read` → 同步到运行变量；收命令更新则 `nv_write` 持久化（配网信息同理，NFC 模块的 NDEF 数据也会掉电不丢）。

7. **这类项目最大的坑是什么？**

> **答**：两类：

- **构建/环境坑**：工具链版本、hb 根目录识别、预编库缺失导致链接阶段不产物；
    
- **运行时坑**：**内存管理**（cJSON、MQTT payload）、**并发与状态冲突**（手动灯/自动灯/长鸣的优先级），以及 **I²C 设备未清屏/残帧** 导致显示脏数据。

**“漂移到底有多大？你怎么量的？”**

- 线程池主循环里已经 10 ms 检查一次，把 `hi_get_us()` 采样值塞进一个 **环形缓冲**（64 点即可），上电跑 1 h 后回读；
    
- 指标：最大 **jitter = (maxΔt – 10 ms)**，**< 500 µs** 算合格（LiteOS-M 时钟节拍的典型值）；
    
    “线程池定时抖动 ≤ 380 µs（1 h 统计，n=360 000）”  
    
    

2. NV 持久化 → 把“掉电不丢”变成“量化寿命”
面试官常追问：  
**“Flash 能写多少次？你这条命令多久写一次？会不会写穿？”**

- Hi3861 内置 SPI Flash，**典型寿命 10 000 次擦除**（查规格书《Hi3861V100 数据手册》表 4-4）；
    
- 你目前做法：命令一下发就 `nv_write`，假设用户每 10 s 调一次阈值，**理论寿命只有 10 000×10 s ≈ 1.16 天**——明显不合理；
    
- 现成补丁：  
    ① 内存变量与 NV 值**异或比较**，只有**真变**才写；  
    ② 加 **5 min 定时合并写**（容忍掉电丢失 5 min）；  
    ③ 简历里给结论：  
    “NV 写放大系数 0.02，**全生命周期可承受 57 年连续参数调整**。”  
### MQTT

“Paho Embedded-C 是我们项目中使用的 MQTT 客户端库。我的工作主要是调用它提供的 API，比如 `MQTTClient_connect` 来连接华为云平台，`MQTTClient_publish` 来上报传感器数据，以及 `MQTTClient_subscribe` 来接收来自小程序的控制命令。我需要确保在正确的时机调用 `MQTTClient_yield` 来维持心跳和处理接收到的消息。”



“选择 Paho Embedded-C 主要是因为它**轻量**，适合 Hi3861 这种资源紧张的设备。它与我们底层使用的 **lwIP 网络协议栈**和 **LiteOS-M** 内核能很好地集成。我的主要工作是在应用层对其进行封装和调用，比如在传感器数据就绪时发布到 `property` 主题，并在一个任务循环中调用 `yield` 来确保能及时收到云端下发的控制指令。我们也处理了网络断连后的重连机制。”


如果面试官问到非常底层的实现（比如：“它的报文组装函数在哪？”），可以坦诚地说：  
“我当时的工作重心在应用逻辑和状态机上。对于 Paho 库，我主要是作为用户，通过它稳定的 API 来完成通信需求。如果涉及到底层协议的修改或定制，我需要去查阅其源码和文档。”

**记住：** 对于应用开发工程师，面试官更关心**如何正确地使用一个库来解决实际问题**，而不是库内部的每一行代码。能说出它的名字、作用、基本工作流程，就已经达到了期望。

# 笔记

> 结构 = 架构 → 启动/并发 → 数据流 → 状态机 → MQTT → NV(Flash) → 显示 → 关键问题与修复 → 构建与联调 → 测试与验收 → 可扩展点 → 文件导览


## 1. 系统架构（文字版图）

- **硬件**：Hi3861，AP3216C（IR/接近），AW2013（RGB），SSD1306（OLED），NFC
    
- **系统**：OpenHarmony（LiteOS-M）
    
- **链路**：传感器采集 → 状态机判定 → 声光（蜂鸣器/红灯） → MQTT 上报/命令下发 → 小程序
    
- **存储**：NV(Flash) 保存 `ps_threshold / timeout_threshold`（掉电不丢）
    
- **并发**：线程池（MQTT 任务/心跳等）+ 采集独立线程

## 2. 启动与并发模型

- **启动顺序**：外设初始化 → NFC 配网 → Wi-Fi → MQTT 连接/订阅 → 线程池启动 MQTT 发送/接收 → 启动采集线程
    
- **线程池**
    
    - 控制结构：任务数组（函数指针 + interval + last_run），互斥保护
        
    - 工作循环：每 10ms 检查是否到期 → 调用；防忙等
        
    - 好处：统一节拍、减少线程数与栈占用，便于新增心跳/重连任务
        
- **采集线程**：独立 while 循环（保证实时性），与线程池互不阻塞

## 3. 数据流与主题

- **上报数据（属性）**：`ps`（接近值）、`ps_threshold`、`timeout_threshold`、`alarm_state`、`isBody`、设备模式等
    
- **下发命令**：设置阈值、开关自动模式、手动蜂鸣（长鸣）、手动灯常亮等
    
- **主题**：遵循华为云设备规范（命令/属性主题模板集中在 `sys_config.h`），避免魔法字符串

## 4. 状态机

- **三态**：
    
    - `ALARM_OFF`：无人或未触发
        
    - `ALARM_TRIGGERED`：有人→进入计时，计时未达阈值
        
    - `ALARM_ON`：超过超时阈值，进入报警（滴滴/长鸣方案）
        
- **关键变量**：`psData`（接近值）、`ps_threshold`、`timeout_threshold`、`is_Body`（是否有人）、手动按钮状态、自动模式开关
    
- **灯/铃优先级**：手动常亮 ＞ 自动闪烁（自动模式 + 自动灯开 + 报警）＞ 仅长鸣可灭灯 ＞ 默认灭灯
    
- **为什么是状态机**：时序清晰、可插入去抖/驻留，易测试和回溯（日志映射状态变化）
    

## 5. MQTT 实现要点

- **客户端生命周期**：初始化 → 连接 → 订阅（命令主题） → 上报属性 → 断线重连
    
- **JSON 处理**：`cJSON` 序列化（上报）/反序列化（命令）；**发布后释放 payload**，异常路径也要释放
    
- **失败策略**：连接失败循环重试（可演进为指数退避）；发布失败记录并可选缓冲重发（后续优化）
    

## 6. NV(Flash) 持久化

- **NV 结构体**：包含 `ps_threshold` 与 `timeout_threshold`（以及扩展字段）
    
- **启动加载**：`read → 校验 → 回填运行变量`，异常时落默认值并 `write` 修复
    
- **命令变更**：设置阈值后 `write` 写回，立即生效并上报
    
- **一致性**：上报 JSON 同步带上当前阈值，云端与设备状态可对照
    

## 7. 显示（OLED）与调试

- **页面**：报警图标/无报警图标、左/右侧“有人/无人与报警状态 Yes/No”、顶部运行信息
    
- **注意点**：每帧前**清空缓冲/区域重绘**，避免“1.5 行显示”和颜色残影
    
- **串口日志**：模块前缀、关键信息（状态切换、参数变更、连接重试、错误码）
    

## 8. 关键问题与修复

- **内存泄漏**：`cJSON_PrintUnformatted` 返回堆内存 → 发布后 `cJSON_free(payload)`；对异常分支同样释放
    
- **类型/显示异常**：`difftime` 返回 `double`，格式化用 `%f` 或先 `int elapsed = (int)difftime(...)` 再 `%d`；并在写屏前清零缓冲
    
- **灯/铃冲突**：统一优先级表，分离“手动常亮”和“自动闪烁”的决策；对“长鸣+自动报警”的过渡态加入去抖或最小驻留
    
- **并发抖动**：改造为线程池统一时基，避免各线程独睡造成的节拍漂移
    
- **构建与环境**：DevEco + hb 工具链与 SDK 版本匹配；缺库/链接错误逐项补齐；容器与本机路径一致性


| 问题                         | 可能原因              | 快速解决                                         |
| -------------------------- | ----------------- | -------------------------------------------- |
| `target.elf` 找不到           | 构建中断 / Ninja 缓存损坏 | `hb clean && hb build -f`                    |
| 编译卡住                       | 缺少交叉编译工具链         | 检查 `device/hisilicon/hi3861/sdk_liteos` 是否完整 |
| hb 报错 “set product failed” | 没有选择产品配置          | 重新执行 `hb set`                                |
| 烧录失败                       | 未连接 J-Link / 串口占用 | 检查设备连接并重启 IDE                                |
|                            |                   |                                              |



## 9. 构建与联调

- **工具**：DevEco Device Tool（含 hb 工具链）
    
- **流程**：清理 → 构建 → 烧录 → 串口观察（初始化 → NFC 配网 → Wi-Fi → MQTT 连接/订阅 → 上报/下发）
    
- **常见错误**：
    
    - 目标 `target.elf` 不存在：构建中断或路径不一致（清理后全量构建）
        
    - 链接库格式不识别：SDK 版本不配或库缺失 → 同步正确版本
        
    - 连接失败：AP/密码/NFC 配网检查、热点频段、Broker 可达性


## 10. 可扩展点

- **MQTT 健壮性**：指数退避、QoS1、发送缓冲队列
    
- **状态机增强**：滑窗平均与去抖、最小驻留、报警节流
    
- **日志与错误码**：统一宏与等级；关键路径埋点
    
- **线程池**：漂移校正、最小间隔、任务启停 API
    

## 11. 文件导览（代码中的锚点）

- `smartSecurityDefense_demo.c`：启动流程（外设/NFC → MQTT → 线程池/采集线程）
    
- `sensor_collect_task.c / .h`：AP3216C 采集、状态机与本地声光
    
- `mqtt_send_task.c / mqtt_recv_task.c / .h`：属性上报、命令解析（cJSON）
    
- `thread_pool.c / .h`：单工人线程，定时任务调度
    
- `sys_config.h`：MQTT 主题模板与常量









## 过程记录
在我的ubuntu22.04上安装**DevEco Device Tool**，但是这个.sh脚本文件需要的python是3.8的，太老了只能手动安装脚本所需要的依赖，在网上找到Ubuntu22.04 搭建 OpenHarmony 命令行开发环境的文档，是使用命令行工具来安装的，但是在使用apt-get命令安装后续操作所需的库和工具的时候发现**软件包依赖冲突**，**尝试自动修复**之后再手动干预还是不行，就只能最小化安装，要安装半小时，而且还不知道缺的那几个工具后面会不会导致失败，ok，失败了，直接用docker来搭建，
最后用docker套了壳安装完成了环境，就是官方文档里docker的开发环境配置

起来重新检查开发环境，发现环境很有问题，结果是进错了容器，昨天的容器里拥有完整的源码和构建系统但是没有专门的编译器，重新下载就没问题了，但是一个对话硬是说完全报废了，另一个对话则只是说重新补足编译器就行了
没能补完，OpenHarmony源码树缺少必要的第三方组件，源码同步成功但是不是需要的分支，正在重新下，下完了，但是hb工具有问题，hb工具检测不到源码根目录
后面发现问题出在hb工具的根目录检测逻辑上，之后发现链接阶段失败，预编译库 `libhistreaminglink.a` 在设备SDK中，但链接器无法识别其格式，就是SDK重新同步后缺少关键的预编译库，
弄了很久，缺了一些关键工具，修复组件又有缺失，填不完，这个版本的ubuntu不行

docker 
docker images 查看已有镜像，docker ps -a 查看容器 
 用查到的 CONTAINER ID 或 NAMES 替换 <container_id>
docker start <container_id>
docker exec -it <container_id> /bin/bash